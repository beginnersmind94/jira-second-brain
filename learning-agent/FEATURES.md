# Learning Studio — Feature Inventory (state of the build)

**Last updated:** 2026-06-13 · **Branch:** `main`
**This is the canonical "what exists" doc.** It is meant to be read by a human OR handed to another LLM
for consultation. **Update it whenever a feature lands, changes status, or is parked** — it is the single
source of truth, ahead of memory or chat history.

> Companion docs: `CLAUDE.md` (operating rules + enforcement vs. convention), `HANDOFF.md` (run gotchas +
> next steps), `docs/` (eval reports), and the plan at `~/.claude/plans/splendid-forging-rossum.md`.

---

## What Learning Studio is
An LMS for K-12 child-nutrition staff. Two halves: a **content factory** (turn transcripts/PDFs/Jira into
fact-checked learning content) and an **LMS / fulfillment engine** (assemble content into tracks, assign to
learners, measure completion). The deliverable is the published, *trustworthy* learning library.

## The one non-negotiable principle (the moat)
**Grounding by construction.** Every product-fact claim must trace to a verbatim source span, or it is cut.
The model emits citation ids; a deterministic assembler renders the verbatim quote + trust tier; a gate
re-checks. This holds for guides AND quizzes. **Manual control = manual *assembly* of curated content; it
never means hand-authoring product claims.** A feature that lets a human type an un-cited product claim is a
moat violation and does not ship. The header badge "✓ every claim cited" is this promise, made visible.

## How to run (demo)
- Server: from `jira-brain/learning-agent`, run `python demo_app.py` with the **sibling** `.venv`
  (`Financials-Documentation-KT/learning-agent/.venv/Scripts/python.exe`) — Windows ProactorEventLoop, port
  **8001**. Static `index.html` is served fresh from disk (frontend edits are live; **backend edits need a
  restart**). Auth is via the `claude` CLI, not `.env`.
- Evals: `python -m eval.report` (offline dashboard); `--live` variants need `claude` auth.

---

## Status legend
**Built** = real & working · **Façade** = seeded demo data, no real persistence/identity (fine for demo) ·
**In progress** = being built now · **Planned** = specced, not built · **Parked** = deliberately deferred ·
**Out of scope** = different project.

## BUILT
| Area | What | Real vs Façade | Where |
|---|---|---|---|
| Educator insight panel E3-lite | Completion funnel (CSS `width:%` bars, amber highlight on >15% drop), question-difficulty list (colored pct pills: ≥80% green / 60–80% yellow / <60% red), summary chips (enrolled · completion% · avg days). Seeded demo data at `data/analytics/track-insights-demo.json` (internally documented as seeded; `_note` field required). `GET /api/analytics/tracks/<tid>/insights` merges seeded JSON with real `completion_store` lesson_done records where they exist. Required `_data_notice` banner in every response and in the UI panel. Aggregate-only: no per-learner drill-down (`user_id` stripped from response by construction). Director + trainer only (403 for cashier/learner roles). "📊 Insights" button in My Team toolbar and in Tracks view real-track cards. 5-test suite covering: arrays present, `_data_notice` SHOULD-NOT-OCCUR, no `user_id` SHOULD-NOT-OCCUR, cashier 403, seeded file on disk. | **Seeded demo data (documented) — real event wiring post-ANC** | `demo_app.py` `api_track_insights` + `_load_insights_seeded` + `_merge_real_completions`; `data/analytics/track-insights-demo.json`; `index.html` `openInsightPanel`/`closeInsightPanel`/`_renderInsightBody` + `#insight-overlay`; `eval/test_insights.py` (5 tests) |
| SCORM 1.2 export + xAPI emission | `GET /api/tracks/{id}/scorm` returns a zip containing `imsmanifest.xml`, SCO launch page with SCORM API shim, per-module HTML content, and quiz JSON. xAPI `completed`/`progressed` statements emitted on cert + module-done; stub mode logs to `logs/xapi-stub.jsonl`; production POSTs to LRS when `LRS_ENDPOINT`+`LRS_KEY` set. Trainer view has "Export as SCORM" button. All non-fatal: LRS/zip errors never block cert issuance. | **Real (scaffold — stub LRS by default)** | `scorm_export.py`, `xapi_client.py`, `demo_app.py`, `index.html` `tbExportSCORM`, `tests/test_scorm_xapi.py` |
| Auth / SSO scaffold | `CurrentUser` dataclass + `get_current_user()` FastAPI dependency; demo bypass via `X-Demo-User` header (john-cashier · dana-director · sam-trainer); production SSO hook stubbed per ADR-001 §Auth; `/api/roster`, `/api/certificates`, `/api/tracks` wired through identity | **Real (interface + demo bypass; production JWT pending)** | `auth.py`, `demo_app.py`, `tests/test_auth.py` |
| SCORM export + xAPI | SCORM 1.2 package export (`imsmanifest.xml` + SCO launch page + module HTML + quiz JSON). xAPI stub logs to `logs/xapi-stub.jsonl`; production writes to LRS when `LRS_ENDPOINT` + `LRS_KEY` are set. "Export as SCORM" button in track builder (trainer view). | **Built (V2)** | `scorm_export.py`, `xapi_client.py`, `demo_app.py` `/api/tracks/{id}/scorm` + `/api/xapi/status`, `index.html` `tbExportSCORM` |
| Generation | Transcript/PDF → grounded guide (Tasks 1–4, one SDK call) | **Real** | `demo_app.py` `/generate`, `demo_d.py`, `agent_sdk.py` |
| Grounding gate | Verbatim citation + correct tier; deterministic publish gate; pinned by REG-01…16 | **Real (enforced)** | `demo.validate_citations`, `eval/regression.py` |
| Human review + AI edit + approve | Grounding-safe find/replace edits; approve re-validates live; audit log | **Real (enforced)** | `revise.py`, `demo_app.approve_resource` |
| Skill assessment / track exam (B3) | Assessment entity assembled from approved question bank (pointer format `<quiz_id>:<idx>`), auto-assemble (type-diverse, ≥1 MCQ/TF/FITB when available), timed taker (visible MM:SS countdown, auto-submit on expiry), server-side scoring reusing `quiz_store.score_quiz()`, attempt history (disk-backed per-user), attempt-limit hard gate (409), certificate gate on track (`assessment_gate_id` field), source quotes withheld during attempt (revealed only in results), 7-test suite | **Real** | `assessment_store.py`, `demo_app.py` `/api/assessments/*` + `/api/tracks/{tid}/assessment-gate`, `data/assessments/`, `data/assessment-attempts/`, `quizzes/quiz-seed-cashier-demo.json`, `data/assessments/assessment-demo-cashier-onboard.json`, `openAssessmentTaker`/`openAssessmentBuilder`/`_atSubmit`/`_abSave` in `index.html`, `eval/test_assessment_store.py` (7 tests) |
| Quiz builder + player + gate | Generate **MCQ, True/False, Fill-in-the-blank, Step-ordering** questions — all gate-grounded to verbatim source spans; drop any question whose span isn't found verbatim; take + score all types with per-question source feedback | **Real** | `qbank_gate.py` (`_gate_tf`, `_gate_fitb`, `_gate_ordering`, `gate_question_by_type`), `quiz_store.py` (type registry, per-type `validate_shape`/`score_quiz`), `/api/quizzes/*`, `openTaker`/`takerPickTF`/`takerCheckFITB`/`takerOrderMove`/`takerSubmitOrdering` in `index.html` |
| Quiz-from-guide | Generate a grounded quiz from a published guide | **Real** | `demo_app.py` `/api/resources/{rid}/quiz` |
| Library assistant | Grounded Q&A over all guides — verbatim-cited answer or "not in the library"; never invents | **Real** | `demo_app.py` `/api/library/ask`, rail in `index.html` |
| First-run onboarding (D2) | Role-aware **welcome overlay** (zero LMS jargon: "LMS"/"SCORM"/"xAPI" banned from overlay copy; "module" → "lesson" in all learner-visible count strings). `GET /api/users/{uid}/onboarding-state` returns `is_first_visit`, `role`, `assigned_tracks`, `recommended_track`, `completed_any_lesson` — `is_first_visit` derived from completion records, so demo reset auto-resets it (no persistent flag). Overlay fires when `is_first_visit && !sessionStorage.onboarding_skipped`; "Start learning →" opens course player; "Skip for now" sets session flag. Track hero card with SVG progress ring (r=18, stroke-dasharray). Empty states on all 4 surfaces: Learn (no tracks), Library (no guides), Quiz catalog (no quizzes), Course player rail (no lessons). `updateTabVisibility(role)` — cashier sees learn+library only; director adds myteam; trainer sees all. 6-test suite. | **Real** | `demo_app.py` `/api/users/{uid}/onboarding-state`; `index.html` `showOnboardingOverlay`/`obStartLearning`/`obDismiss`/`updateTabVisibility`/`renderTrackHero`/`_d2PostRenderLearnHome`/`_d2PatchLibraryEmpty`/`_d2PatchCatalogQuizEmpty`; `eval/test_onboarding.py` (6 tests) |
| Learn (3-level) | DataCamp-style **Track → Courses → Lessons** (guide HTML inline / grounded quiz in-player / video C1 card / flashcard stub / exercise stub / certify); per-track seeded progress, complete → certify; Customer lands here. **Real published tracks built+assigned this session now appear here, filtered by a "Viewing as <role>" selector**; opening one launches the **unified course player (D1)**: left rail (courses → lessons with checkmarks + current highlight; collapses to horizontal strip at ≤768px) + inline lesson pane; lesson-level progress persisted to disk; resume = first incomplete lesson; auto-advance countdown (5s); track-complete celebration + "Get certificate" button. Origin badge on every pane type. "Show source" on ai_grounded lessons. Citation comments stripped server-side (`/resources/{rid}/html`). Trainer chrome unreachable from inside player. Performance marks: `player-start`/`player-ready`/`player-load` (<3s NFR measured). Legacy demo tracks still use `openTrack()` flat player — unbroken. | **Real — unified course player (D1): left rail + inline lesson panes (guide HTML, quiz in-player, video C1 card, flashcard stub, exercise stub); lesson-level progress + resume; auto-advance; <3s measured render** | `index.html` `openCoursePlayer`/`_cpRender`/`_cpRenderLesson`/`_cpMarkDone`/`_cpOnLessonComplete`/`closeCoursePlayer`/`renderLearnHome` · `completion_store.py` `set_lesson_done`/`get_lesson_done` · `demo_app.py` `/resources/{rid}/html` + `/api/tracks/{tid}/lesson-progress` · `eval/test_course_player.py` (6 tests) |
| Trainer Home / day-view | 4 KPI tiles, 5 district project cards (Aldine "raised hand" flag + Respond), "N approved by PM team" inbox → open PDF / send to Catalog. Post-demo role audit: all content-event actor/action pairs verified consistent with PO-as-approver / Jaime-as-router model. | **Façade data** | `index.html` `loadHome`/`renderHome`/`respondHelp` |
| Manage (Trainer) | All-6-districts roll-up: completion %, logins, status, Aldine help banner, expandable per-district rows → Roster | **Façade data** | `index.html` `loadManage`; `demo_app.py` `/api/roster` + `_district_stats` |
| My Team (CN-Director) | Customer-scoped staff compliance by site/role vs deadline; nudge one / nudge-all-overdue; self-assign a track | **Façade data** | `index.html` `loadMyTeam`/`nudgeOne`/`doAssign` |
| Compliance export (My Team) | Download compliance report (PDF) button (Customer view only); GET /api/districts/{isd}/compliance-report (JSON) + /pdf (PDF with DEMO DATA watermark); seeded data, 9-test suite. Wire to real roster once SSO + roster sync land (V1.5, Tasks 11-13). BRD FR-RP-03. | **Built (seeded data, PDF export)** | demo_app.py _build_compliance_report + 2 routes; index.html downloadComplianceReport; tests/test_compliance_export.py |
| Tracks + Courses builder | Trainer assembles **courses** from approved guides + auto-grounded quizzes + video, reorder, Save → builds **tracks** from courses; appears in Learn. No free claim-authoring (moat-safe) | **Real — rewired to `/api/courses/*` endpoints (A2); lesson pickers show approved-only content; ICN video picker split by license posture (embed / link-out); preview-as-learner modal shows same trust chrome as real learner; ≤8-click publish path; server 409 backstop for 0-lesson publish + 422 for draft refs; description field labeled human-authored; `_demoState.userCourses` replaced by real API state** | `course_store.py`, `demo_app.py` `/api/courses/*`, `data/courses/*.json`, `eval/test_course_store.py`, `eval/test_course_builder_ui.py`, `index.html` `openCourseBuilder`/`cbSave`/`cbPublish`/`cbPreview`/`openTrackBuilder`/`tbkSave` |
| ICN video embedding (C1) | License-aware ICN embed: youtube-nocookie iframe for `embed_only`/`download_allowed`; link-out card for `link_only`. Attribution band structural (non-removable). Offline Wi-Fi fallback (thumbnail+link after 6s or `OFFLINE_DEMO=1`). `cbAddVideo()` replaced by ICN picker — no raw URL entry, lesson stores ICN asset ID (`ref`), never a URL. `EXTERNAL_LEARNING=0` disables gracefully. | **Real — license-aware ICN embed (youtube-nocookie) or link-out card per `license_posture`; attribution band non-removable; offline fallback chrome** | `demo_app.py` `/api/icn`, `/api/icn/<id>`, `/api/config` (`offlineDemo`/`offline_demo`); `index.html` `renderVideoCard`/`loadAndRenderVideoCard`/`cbAddVideo`/`cbPickVideo`; `data/icn/data/`; `tests/test_video_embedding.py` |
| Guide flashcards (B2) | **Guide flashcards** generated from published approved guides with same grounding-by-construction guarantee as quizzes: verbatim `source_quote` gate (cards that fail are dropped), `source_content_hash` drift detection (guide edit reverts approved deck to draft), approval endpoint re-runs gate live, status always `"draft"` on creation. Procedural cards preferred. Card-flip learner UI (CSS `rotateY` 150ms, "Got it / Review again" sorting, progress indicator, source quote on back, touch swipe). Course builder `+ Flashcards` picker (approved decks or generate-and-add). `course_store.validate_lesson_ref()` extended for `flashcards` type. Seed demo deck pre-approved. **ICN flashcards** also real (on-demand, not persisted). | **Real — `flashcard_store.py`; `POST /api/resources/{rid}/flashcards`; `POST /api/flashcards/{id}/approve`; `POST /api/flashcards/{id}/check-drift`; `GET/DELETE /api/flashcards/*`; deck attachable to courses as lesson type; seed at `data/flashcards/deck-20260612-demo-seed.json`; 7-test suite** | `flashcard_store.py`, `demo_app.py` `/api/resources/{rid}/flashcards` + `/api/flashcards/*`, `course_store.py` (flashcards validation), `index.html` `_loadFlashcardDeck`/`openFlashcardDeck`/`cbAddFlashcards`/`cbGenFlashcards`/`cbPickFlashcards`/`_fdRender`/`_fdFlip`/`_fdSort`, `eval/test_flashcard_store.py` |
| Library vs Catalog (IA) | **Studio** = review worklist over the real `/resources` set (drafts + approved) + published tracks; **Catalog** = ready-to-use shelf over real `/api/modules` (AI + Cybersoft guides + ICN) + published tracks. Origin/status badges; Source filter; ICN claim is now TRUE (wired in), not over-claimed | **Real backend** (adapters reuse existing filter/render) | `index.html` `loadLibraryDemo`/`loadCatalogDemo` |
| ICN into tracks | ICN_DOC modules are addable in the track builder and expand inside a track (learner can open the credited ICN reference; never reproduced) | **Real** | `demo_app.py` `/api/modules`+`/api/tracks/{id}` pass `icn_dir`; `modules_store._load_icn_modules` |
| Quality (top-level) | Eval/quality strip + per-item rubric scores (Grounding=deterministic gate, Coverage/Clarity/Structure=`scorers.py` heuristics), SME verdict states, cited-claim drill-down | **Real (scorer-based) + Façade fallback** | `scorers.py`, `index.html` `loadQuality`/`qualityDetail`/`_qlScoreArray` |
| Persona nav + provenance | Avatar "Viewing as" dropdown toggles Trainer/Customer tab sets + safe-to-share banner; canonical `.origin-badge` system (`.ai-grounded` · `.human-authored` · `.outside-vendor`) applied consistently across all content surfaces: Library detail panel, Catalog cards + detail drawer, track module rows + hero path-stats, ICN path detail, quiz taker kicker. Over-claiming prevented by construction — `originBadgeHtml()` is the sole emitter; callers never hard-code badge text; human-authored and outside-vendor cards never receive "every claim cited" text. "Show source" popover (fetches `citation_integrity` metadata) present only on `ai_grounded` content. Coverage matrix: `docs/jun12/PROVENANCE-COVERAGE.md`. Tests: `eval/test_provenance.py` (35 assertions, 0 failures). | **Built (F1 — Wave 1)** | `index.html` `originBadgeHtml`/`showSourcePopover`/`openCatalogDetail`/`reviewDemoItem`/`openTaker`/`openPath`/`_renderRealTrack`; `eval/test_provenance.py`; `docs/jun12/PROVENANCE-COVERAGE.md` |
| Multi-tenant isolation | API-layer district-access enforcement via `tenancy.py` `assert_district_access()` on all district-scoped routes (`/api/roster`, `/api/modules`, `/api/tracks`). Trainers scoped to their book-of-business; learners/directors to their own district. 403 on any cross-district read. Seed data is no longer the only tenant boundary. | **Built (API-layer enforcement)** | `tenancy.py`, `auth.py`, `demo_app.py`, `tests/test_tenancy.py` (18 tests) |
| Roster / multi-tenant | 6 districts + per-learner progress/status; ISD switcher reconciled to demo universe | **Façade data** | `demo_app.py` `/api/roster`, `loadRoster`, `_demoISDInit` |
| Roster sync + completion writeback | `RosterSyncClient`: seeded roster in demo mode; writes completion records to `logs/writeback-stub.jsonl`. Production: set `SCHOOLCAFE_API_URL` + `SCHOOLCAFE_API_KEY`. `/api/tracks/{id}/progress` fires `sync_completion()` after progress update; `/api/certificates` fires with `certified=True`. Non-fatal: failures log and continue. `/api/sync/status` shows live vs stub. | **Built (stub mode)** | `roster_sync.py`, `tests/test_roster_sync.py` (17 tests) |
| Imported guide library | 86 SchoolCafé/ICN guides as approved Library items, honest provenance (human-authored, **not** gate-grounded); Source filter | **Real content, not gate-grounded** | `import_guides.py`, Library view |
| PDF → markdown upload | Server-side transcript conversion (header/footer strip, multi-column warning, 80pp cap) | **Real** | `pdf_to_md.py`, `app.upload_transcript` |
| Eval suite | See **Evals** below | **Real** | `eval/*` |
| Conference demo mode | `DEMO_MODE=conference` env flag (NEVER set in production — suppresses honest labels). When set: (1) watermarks / "DEMO DATA" banners suppressed on seeded artifacts (compliance PDF, resource PDF pending-review banner); (2) boot pre-warm loads all library docs + ICN catalog so first load is instant; (3) `POST /api/demo/reset` (conference-mode-only, 403 otherwise) clears all demo user completion records + certs + leads log in <10s; (4) operator-only reset UI in the browser — invisible to visitors, activated by `?operator=1` URL param or triple-R keyboard chord on a hidden anchor; (5) generation-theater replay: if `data/demo/generation-replay-<rid>.jsonl` exists, `/generate` streams it line-by-line instead of hitting the LLM; (6) `/api/config` exposes `conferenceMode: true` + `window._config` set globally. Default (flag off) = today's honest-watermark behaviour — unchanged. | **Real** | `demo_app.py` (`CONFERENCE_MODE`, `demo_reset`, `_conference_prewarm`, `_stream_replay`); `completion_store.reset_demo_users`; `static/index.html` (`_initOperatorReset`, `_showToast`); `.env.example`; `eval/test_demo_mode.py` (5 test cases) |
| Certificate polish (D7) | Redesigned frame-worthy cert layout (max-width 480px, brand accent bar, clean serif name, badge strip, score line). Schema upgrade: `score_pct`, `passed_assessment`, `assessment_title`, `user_display_name`, `badges_earned` (from D3 gamification store), deterministic `verification_code` (CYB-XXXXXXXX, SHA-256 of cert_id). Public `GET /api/certificates/verify/<code>` returns `{found, user_display_name, track_title, issued_at}` — no user_id, no district, no other PII (field-by-field construction enforced). `GET /api/certificates/<cert_id>/pdf` — weasyprint PDF if available, pymupdf fallback, HTML download as last resort; always 200 with Content-Disposition attachment. Download button calls PDF endpoint; Share button uses Web Share API on mobile (copy-link fallback). One-time CSS shimmer animation on cert border (1.5s, stops). `showCertificate()` upgraded to load real cert from API, falls back to facade for legacy flat tracks. 5-test suite. | **Real** | `completion_store.py` (`issue_certificate`, `get_certificate_by_verification_code`, `_verification_code`); `demo_app.py` (`verify_certificate`, `certificate_pdf`, `_build_cert_html`); `static/index.html` (`_renderCertificate`, `_certDownload`, `_certShare`, `showCertificate`, CSS cert redesign); `eval/test_certificate.py` (5 tests) |
| Production readiness docs | Env vars, integration checklist, stub→real activation steps for all 7 V1.5 modules (auth/SSO, roster sync, xAPI/LRS, completion store, tenancy, SCORM, ICN flag), zero-to-prod checklist, honest stub-vs-real summary table, and remaining code-change requirements (SSO JWT, DB backend, demo-seed removal) | **Docs** | `docs/PRODUCTION.md` |
| Gamification (D3) | XP + streaks + completion badges. Lesson completed: 10 XP; quiz passed: 20 XP; assessment passed: 50 XP; track completed: 100 XP bonus. Daily streak (any completed lesson sustains; missed day resets; server-side — device-agnostic). Badges: per-track completion (`"<title> Complete"`), `"First Lesson"`, `"On a Roll"` (3-day streak), `"Scholar"` (first assessment passed). Learner home gamification strip (flame · XP · badge chips · next-badge hint). Post-lesson XP toast (+10 XP · streak, slides in from bottom-right, 3s dismiss). Certificate badge strip (earned badges rendered below learner name). Manager aggregate view: `/api/roster` includes `gamif_summary: {avg_xp, total_streak_days, badges_awarded}` for director/trainer — no per-learner rankings, no leaderboard (DEC-4 trust guardrail). Demo reset clears `data/gamification/<uid>.json`. 6-test suite (6/6 green). | **Real** | `completion_store.py` (`add_xp`, `update_streak`, `award_badge`, `check_and_award_badges`, `get_gamification`; `data/gamification/*.json`); `demo_app.py` (`GET /api/users/{uid}/gamification`; hooks in `/api/tracks/{tid}/lesson-progress`, `POST /api/assessments/{aid}/attempt`, `POST /api/certificates`; `gamif_summary` in `/api/roster`); `index.html` (`renderGamificationStrip`, `showXpToast`, `_cpMarkDone` toast hook, `_renderCertificate` badge strip, `#learn-gamif-wrap`, `.gamif-strip`/`.badge-chip`/`.xp-toast` CSS); `eval/test_gamification.py` (6 tests) |
| Track assignment / deadlines (A3) | Assignment entity persisted in track JSON (`assignments: [{audience_type, audience_value, district, due_date, assigned_at, assigned_by}]`). `GET /api/tracks/{tid}` injects `due_date`, `days_remaining`, `is_overdue` for the applicable assignment (role > district > user priority). `POST /api/tracks/{tid}/assign` (trainer-only), `DELETE /api/tracks/{tid}/assign/{idx}`, `GET /api/tracks/{tid}/assignments`. Due-date chip in learner hero and course player top bar: neutral (>7 days) / amber (1–7 days) / red (overdue) / hidden (complete). Prerequisite lock: `prerequisites: [track_id]` in track JSON triggers `locked: true, locked_by: {track_id, title}` in `GET` response; banner in course player pane and track detail view. Milestones field (cosmetic, non-blocking). Assign builder UI (`openAssignBuilder(tid)`) — audience dropdown + date picker + Assign (≤3 interactions); "✋ Assign" button in track builder alongside Publish. Seed: Cashier Onboarding track has Houston ISD cashier assignment due 2026-07-31. 6-test suite (T1 persist, T2 cashier due fields, T3 past-due SHOULD-NOT-OCCUR, T4 complete not overdue, T5 prereq lock, T6 wrong-role SHOULD-NOT-OCCUR). | **Real** | `demo_app.py` (`_find_applicable_assignment`, `_assignment_due_fields`, `_check_prerequisites`, `POST/DELETE/GET /api/tracks/{tid}/assign*`); `data/tracks/track-20260610-230739-c0b779.json` (seeded); `index.html` (`_dueChipHtml`, `openAssignBuilder`/`abCommit`/`abRemove`/`abClose`/`abUpdateCount`, `tbAssignTrack`, lock banner in `_cpRender` + `_renderRealTrack`, due chip in `renderLearnHome` + `_cpRender`); `eval/test_assignments.py` (6/6 tests) |
| Demo content library (G1) | 3 showcase tracks: **"New Cashier Onboarding"** (2 courses, 5 lessons: approved Item Management guide + embed_only ICN YouTube video + cashier quiz + flashcard deck + timed assessment), **"Manager Essentials"** (1 course, 3 lessons: approved Eligibility guide + link_only ICN resource card + manager quiz), **"3-Minute Food Safety"** (1 course, 2 lessons: embed_only ICN YouTube + 3-question quiz, cert on completion, total ≤3 min). ICN catalog: `embed_only` YouTube asset verified, `link_only` manager resource verified. Role-filtered Learn home: cashier sees tracks 1+3, director sees all, trainer sees all. `course_store.validate_lesson_ref()` extended with `"assessment"` type (resolves to `data/assessments/<ref>.json`, must be published). 6-test suite (6/6 green). | **Real** | `data/tracks/track-g1-*.json`, `data/courses/course-g1-*.json`, `quizzes/quiz-seed-manager-essentials.json`, `quizzes/quiz-seed-food-safety-3q.json`, `course_store.py` (assessment type), `eval/test_showcase_library.py` |
| Practice mode (D4) | Spaced-repetition daily sessions (5 items, approved flashcards + missed questions, SM-2-style review queue), streak integration (D3 hook — non-fatal no-op until D3 ships), session persistence in `data/practice/<user>.json`, 6-test suite. Trust guardrails enforced by construction: `build_session()` checks `deck.status == "approved"` and `quiz.status == "approved"` before including any item; source quote shown with every practice item; no practice items invented. Pool = real approved flashcard decks + approved quiz questions across all tracks. Missed questions added to `review_queue` with `next_review = today + 1`; dedup window = 7 days. Practice tab visible to all learner roles (cashier, director); hidden from trainer nav. Session card: "Today's Practice · N items · ~5 min" + Start Practice button; completed-today state shows check + "Review again?"; empty state when no approved content. Practice taker: flashcard flip (reuses `_fdRender` pattern + source quote on back), question render (MCQ/TF/FITB/ordering via `_ptRenderQuestion`), per-item correct/incorrect feedback with source quote, wrong answers fire `POST .../practice/missed`, last item fires `POST .../practice/complete` + XP toast. Progress bar "Item N of 5". All touch targets 44px. Seeded demo session at `data/practice/john-cashier.json` (3 flashcards + 2 quiz questions, `completed: false`). | **Real** | `practice_store.py`, `demo_app.py` `/api/users/{uid}/practice` + `/api/users/{uid}/practice/complete` + `/api/users/{uid}/practice/missed`, `data/practice/john-cashier.json`, `index.html` `loadPractice`/`openPracticeSession`/`_ptRenderItem`/`_ptRenderFlashcard`/`_ptRenderQuestion`/`_ptShowFeedback`/`_ptFinish`/`closePracticeTaker`, `eval/test_practice.py` (6 tests) |
| Mobile / performance (D6) | 375px audit + fix: `overflow-x:hidden` on body; 44px `min-height` on all taker/player/nav buttons (`.quiz-opt`, `.ordering-item`, `.ordering-move`, `.cp-btn-prev`, `.cp-btn-next`, `.cp-rail-lesson`); 16px `font-size` on `.fitb-input` + text inputs (iOS no-zoom); `touch-action:manipulation` on `.fd-card-wrap`/`.fd-card`; `min-height:44px` ordering items; MCQ options full-width (`display:block;width:100%` — already set, padding bumped to 12px 14px). Trainer-chrome lazy-hide: `data-trainer-only="true"` on `view-author` + `view-tracks`; `applyViewMode()` toggles `.trainer-lazy` (CSS `display:none !important`) — learner path skips trainer DOM cost on first paint, un-hides on trainer switch. WCAG 2.1 AA: `.origin-badge.human-authored` darkened to `#6b4f0b` (~5.3:1 on `#fff3cd`); `[tabindex]:focus-visible` added alongside existing `button:focus-visible` ring; `aria-label` on `.cp-back-btn` ("Back"), `.cp-btn-prev` ("Previous lesson"), `.cp-btn-next` ("Next lesson"/"Finish course"); `#cp-player` gets `role="dialog" aria-label="Course player"`; rail promoted to `<nav aria-label="Course lessons">`. Swipe hint: `.cp-swipe-hint` injected into `_cpRender` HTML (absolute, bottom-68px, shown only at ≤768px via media query); shown 3s after player-load on first visit, then auto-dismissed + `localStorage.setItem('cp_swipe_hint_shown','1')`. Performance: `window._playerLoadMs = Math.round(duration)` exposed after `performance.measure('player-load')`; `console.warn` if >3s SLA. 5-test structural/server suite. | **Real** | `index.html` (CSS + `applyViewMode` + `_cpRender` + `openCoursePlayer`); `eval/test_mobile_perf.py` |

## Evals (what's measured + latest, with honesty caveats)
- **Regression (REG-01…16):** pins the grounding guarantees. `eval/regression.py`. **Gating, deterministic.**
- **Support judge calibration:** `eval/judge_eval.py` — 34-case gold set. After the `c05` prompt fix, **live FNR 0% / FPR 6.67%** (`docs/JUDGE-CALIBRATION-2026-06-08.md`). *Caveat: model-prompted, advisory — the deterministic gate is the floor; the 2 held-out generalization cases are same-author, not a blind holdout.*
- **Source-faithfulness:** `eval/source_faithfulness.py` — guide-claim ⊆ cited span. Caught the **real Q4 guide↔transcript drift** live (FN 0% on the fixture). Catches what the verbatim gate + quiz gate structurally can't.
- **Quiz reliability sweep:** `docs/QUIZ-RELIABILITY-INV-DISTRIBUTION.md` — 10× on one guide: 59/60 kept, **0/10 fully-clean runs**, ~2–3 real wrong-keys after triage. The verbatim gate passed all; only the support judge caught them.
- **Trace-back audit:** `docs/TRACE-BACK-AUDIT-INV-DISTRIBUTION.md` — a **standing manual SME gate** (the backstop for the judge's residual FN). **Scaffolded, NOT yet SME-run.** "0 automated flags ≠ safe until trace-back runs."
- **Scorer unit tests:** `tests/test_scorers.py` — 42 cases for `score_coverage`, `score_clarity`, `score_structure`, `compute_scores`, `_extract_headings`, `_count_words`. All deterministic, no SDK deps. Runs with `python -m pytest tests/test_scorers.py -v`.
- Also: triage classifier (`eval/triage_eval.py`), scope/lane-purity (`eval/scope_eval.py`), coverage, source-quality, `eval/report.py` (Jaime dashboard).

## IN PROGRESS
Nothing open. The **complete end-to-end seeded demo** shipped this session — all 7 surfaces (Home, Learn,
My Team, Manage, Quality, Tracks/Courses builder, Library/Catalog) are fully populated on a client-side
`_DEMO` universe (6 districts, seeded learners, courses, tracks, inbox, quality items) and walk-through-ready.
The 4 prior to-dos (nav re-carve + provenance · projects/allocation · builder · day-view/inbox) all landed.
Next: the Thursday live walkthrough + whatever the user flags.

## PLANNED / PARKED (deliberately deferred, post-Thursday)
- Full **Studio / Deployments / Insights** IA rebrand (use as a grouping model, not a rename).
- Full-page **deep-authoring editor** (moat risk — only a *sequencing + grounded-generate* surface, never a free canvas).
- Drag-and-drop reordering; background "Selection Mode" click-to-append; comms/broadcast hub; multi-select floating action bars; heavy tactile visual polish.
- Real infra (V1.5+): SSO / district rostering, real per-learner persistence + completion-of-record, Chroma vector retrieval (today retrieval is JQL/keyword), the LLM Evaluator (Task 5) + routing, template-regeneration workflow, Word (`.docx`) upload.

## OUT OF SCOPE (different project — do not build here)
- **Financials Ledger export** (Oracle / Tyler Munis via custom-reports). Separate project.

## Honesty notes for any consulting LLM
- "Built" rows tagged **Façade** have **no real persistence/identity** — they reset and use seeded data. Do not represent them as production-ready.
- Imported guides are human-authored and **not gate-grounded**; only AI-generated guides carry the verbatim-citation guarantee.
- Eval numbers above are real measurements with stated caveats; the support judge is **advisory**, the deterministic gate + human approval are the floors.
