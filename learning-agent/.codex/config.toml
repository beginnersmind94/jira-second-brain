# Codex project config for learning-agent.
#
# Custom agents are auto-discovered from .codex/agents/*.toml. Each custom
# agent file uses the documented standalone schema: top-level name,
# description, sandbox_mode, and developer_instructions.

[agents]
max_threads = 4
max_depth = 1
job_max_runtime_seconds = 1800
