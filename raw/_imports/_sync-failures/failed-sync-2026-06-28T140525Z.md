# Jira Sync Failure — 2026-06-28T14:05:25Z

## Failure reason

The Atlassian MCP connector was unavailable at run time. `ToolSearch` was called multiple times with queries targeting Atlassian/Jira tools; zero tools from the Atlassian server were registered in the session. The JQL query could not be executed.

## Steps completed before failure

- Read `CLAUDE.md` and `REPO_STRUCTURE.md` — confirmed directory rules and ingestion protocol.
- Read `.brain-state.json` — captured pre-run counters: `tickets_written=8849`, `tickets_updated=2`, `tickets_unchanged=9` (step 4 totals).
- Listed `raw/_imports/*.csv` — **no pre-existing CSV files** at run start.
- Searched for Atlassian MCP tools: no results returned.
- Called `ListMcpResourcesTool` for server `Atlassian`: no resources found.

## CSVs in raw/_imports/ at run start

None.

## JQL that could not be run

```
issuetype = Story AND ("Module" = "Financials" OR component = "Financials") AND updated >= -4d ORDER BY updated DESC
```

## Action required

Verify that the Atlassian MCP server is correctly authenticated and connected in this session's environment. Re-run the routine once the connector is available.
