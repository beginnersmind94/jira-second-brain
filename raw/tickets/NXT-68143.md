---
key: NXT-68143
summary: "GC- PE and SC-Item Search API logic- Enhancing the GTIN Search criteria to search by GTINs, Product codes, and Vendor item numbers for all Data providers."
status: "Future Merge"
resolution: "Done"
components: ["Global Catalog"]
sprints: ["Release 9.6", "Release 9.7", "Release 9.8", "Release 9.9", "Release 9.10"]
low_signal: false
---

# NXT-68143 — GC- PE and SC-Item Search API logic- Enhancing the GTIN Search criteria to search by GTINs, Product codes, and Vendor item numbers for all Data providers.

## Description
GC- PE and SC-Item Search API logic- Enhancing the GTIN Search criteria to search by GTINs, Product codes, and Vendor item numbers for all Data providers.

## Acceptance Criteria
GC- PE and SC-Item Search logic- Enhancing the GTIN Search criteria to use GTINs, Product codes, and Vendor item number values for all Data providers (API)

# GTIN search should work the same way for all data providers. 
!image-20251106-170700.png|width=360,alt="image-20251106-170700.png"!

# GTIN search logic should consider the Left 4 digits as well. (Currently, from what i remember  its only checking for a match by comparing the right 10 digits) -please confirm if search results are correct when two GTINs different only in the left 4 digits and have the same code in right 10 digits.
# GTIN search should consider all these 5 fields. 
## GTIN, GTINCode, BrokenUnitGTINCode, Vendor_UPC, Vendor_UPCBroken
# search by Product Code search should consider 
## Product code and Stock item Product code values.
# Search by Vendor item number (aka “Vendor Product Code”)  should consider the value given for vendor item number. 
# If the comma separated string has a value that is less than the minimum GTIN  length then dont display items results matching with the given code GTIN 
## Example : if i gave search text as “00003” then we should not see the items with GTIN code as “10030000311827” as it has the given search text in the GTIN Code. 
# If the comma separated string has a value that is less than the minimum GTIN  length then match on the full text instead of partial text. 
## Example: if i gave the search text as “00003” and if there exists an item with Product code as “200003” then we should not see this item in the results. 
## only items with Product code, stock product code or vendor item number fields matching with full search text should be displayed in the results. below are the results for the given search text “00003”
## !image-20251106-180656.png|width=855,alt="image-20251106-180656.png"!
# If the given search text value is more than 9 digits then that text should be searched in all these fields.
## GTIN, GTINCode, BrokenUnitGTINCode, Vendor_UPC, Vendor_UPCBroken, Product code and Stock item Product code values, Vendor item number. 
# GTIN column in the Grid is not displayed if the item has GTIN value saved in these fields : GTINCode, BrokenUnitGTINCode, Vendor_UPC, Vendor_UPCBroken
## We need to fix it and show the GTIN number in GTIN column of the grid. 
## !image-20251106-181437.png|width=622,alt="image-20251106-181437.png"!
