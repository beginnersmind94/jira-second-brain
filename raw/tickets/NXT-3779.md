---
key: NXT-3779
summary: "Create range selection"
status: "New"
resolution: ""
components: ["Account Management"]
sprints: []
low_signal: false
---

# NXT-3779 — Create range selection

## Description
The range selection allows the user to select data between and including two values. The range criteria can be numeric, date, or alphabetical.

## Acceptance Criteria
A upper and lower range can be set.

When only a lower range is set any value greater than or equal to the lower range is a match.

When only an upper range is set any value less than or equal to the upper range is a match.

When both a lower and upper range is set only values between or equal to the range values is a match.

A range value can be numeric

A range value can be a date

A range value can be text

Combinations between range types is not allowed ie (Date and Text, Numeric and text, Numeric and date).

If a range is given in the incorrect order an error is displayed saying 'Lower range value is greater than higher range value, please correct'
