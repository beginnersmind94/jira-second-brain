---
key: NXT-12107
summary: "Accountability - Sessions - Data Grid Cleanup"
status: "Done Done"
resolution: "Done"
components: ["Accountability"]
sprints: ["Perseus Sprint 39 (NXT Board)"]
low_signal: false
---

# NXT-12107 — Accountability - Sessions - Data Grid Cleanup

## Description
As a user I want to see all fields within my grid view (especially any action controls) so that I am not confused on information that is “missing” or how to interact with sessions.

# Remove grid grouping header
# Remove grid filter row
# Remove static values from grid columns to allow for automatic sizing and to avoid horizontal scrolling
# Site/Site Code column should be named to “Site”
# Dates should appear as “MM/DD/YYYY hh:mmaa” (ex - 03/10/2021 3:14pm). Be sure that the district timezone setting.
# Add grid title of “Sessions” instead of showing as blank

## Acceptance Criteria
# Grid grouping header is not present
# Grid filter row is not present
# All selected columns should be displayed within the grid without horizontal scrolling
# Grid header should read as “Site” instead of “Site/Site Code”
# Date formats should reflect the 12 hour instead of 24 hour and display in the format mentioned above
# Grid title should be displayed as “Sessions” instead of displaying as blank
