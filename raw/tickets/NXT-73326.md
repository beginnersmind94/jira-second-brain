---
key: NXT-73326
summary: "FIN — Reverse Import: GL Accounts Import"
status: "Ready for Development"
resolution: ""
components: ["Financials"]
sprints: ["Release 9.17"]
low_signal: false
---

# NXT-73326 — FIN — Reverse Import: GL Accounts Import

## Description
Adds Reverse import to **GL Accounts Import** rows on the [Import & Export Log](https://primeroedgenext.atlassian.net/browse/NXT-73286) page.

Reverse is always available on a Done GL Accounts Import. If all imported accounts have zero posted activity, full reversal deletes everything. If some accounts have activity, partial reversal deletes only the zero-activity accounts — accounts with posted transactions stay.

## Full reversal (no activity on any account)

BL deletes the imported accounts, their [site-level sub-accounts](https://primeroedgenext.atlassian.net/browse/NXT-68312), categories, and all non-ledger dependencies (data point mappings, sub-account mappings, template rules). No journal entries are deleted because none exist. Status → Reversed.

If any imported accounts are mapped to data points, the confirmation dialog lists each and warns that mappings will be removed. The user can reconfigure afterward in Configuration > GL Account Mapping.

If an imported category contains non-imported accounts (manually created after the fact), the category cannot be deleted until those accounts are reassigned. The dialog lists which accounts need to be moved.

## Partial reversal (some accounts have activity)

The confirmation dialog shows two groups: accounts that will be deleted (zero activity) and accounts that will be kept (have posted transactions). On confirm, BL deletes only the zero-activity accounts and their dependencies. Accounts with activity and their dependencies are not touched. Status → Partially Reversed.

After partial reversal, re-importing the correct file recreates the deleted accounts via create/update on Account Code (per [NXT-69421](https://primeroedgenext.atlassian.net/browse/NXT-69421)). No duplicate conflict — the kept accounts are updated in place.

The View details drawer shows which accounts were deleted and which were kept, with guidance: re-import the correct file to fix account details, use a manual journal entry to correct posted entries, or contact Support for larger changes.

## Confirmation dialog

Full reversal: shows import name, counts, and everything that will be deleted. Warning: "This will permanently delete {AccountCount} accounts, {CategoryCount} categories, and all sub-accounts, mappings, and templates listed above. This cannot be undone."

Partial reversal: shows two groups with counts. Warning: "{DeleteCount} accounts have no activity and will be permanently deleted. {KeepCount} accounts have posted transactions and will remain. To fix those accounts, re-import the correct file or reverse their entries with a manual journal entry. This cannot be undone."

Both: mandatory comment (min 10 chars). Buttons: Cancel (default) / Reverse import (destructive).

On confirm, Status → Reversal In Progress → Reversed or Partially Reversed. On failure, Status → Reverse Failed with a Retry button.

## Config page impact

After reversal, the [GL Configurations page](https://primeroedgenext.atlassian.net/browse/NXT-64975) reflects the deletion across all six tabs. System Accounts and Mapped GL Accounts tabs re-surface warnings for unmapped system accounts and data points. System Rules that referenced deleted accounts are flagged as broken with posting blocked until remapped. Imported GL Accounts and Account Categories tabs reflect the current state — deleted records gone, kept records remain.

## Acceptance Criteria
_(none)_
