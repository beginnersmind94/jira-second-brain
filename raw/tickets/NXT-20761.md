---
key: NXT-20761
summary: "Update Accuracy Report"
status: "Done Done"
resolution: "Done"
components: ["Face/Food Detection"]
sprints: ["Perseus Sprint 57(NXT Board)"]
low_signal: false
---

# NXT-20761 — Update Accuracy Report

## Description
As an enhancement, we need to update the Accuracy Report By Date report to correct a misunderstanding when developed.

*Application*: Face and Food Detection Website

*Background:*

* When we created the Accuracy Report By Date page, there was a miscommunication about the dataset and requirements
* What was developed is good and we want to keep it, but we still need the original report as it is critical for communicating success or failure

*Flow:*

* Browse to PrimeroEdge and click the link for Food and Face Detection
* Browse to the Reports
* View data

*Requirements*:

* On the By Day tab, make these changes:
** Change the name to By Transaction instead of By Day
** Can you add the column from the By Menu Item tab labeled Menu Item?
*** If you can, then we can remove the By Menu Item tab since it is the same data for the rest of it
*** if you can’t, then leave that tab alone
** Make these changes to the Grid headers:
*** Items Added to Transaction = Detected
*** Items Detected = Menu Items
*** Trans # = Trans ID
*** Scan # = Scan Count
*** Move Scan # over to in between Items Detected and Accuracy %
*** District can be removed from the default view of the grid, but make sure it is still available in the columns list to be added back in case we need it
** Default to 10 rows instead of 5
** The rest of the popup functionality should stay the same
*  
* Add a new radio button / tab and call it By Day
** After the above changes, copy what is on the By Transaction (grid and filters content) onto the new By Day and makes these changes:
*** Remove Trans # column
*** Remove Detected column
*** Remove Menu Items column
*** If you added the extra Menu Item column, then remove it too
*** Only columns should have: District, Site, Date, Scan Count, Accuracy %
*** Have only one row per day
*** Scan count and Accuracy % are for the whole day
**** sum or average for whole day as needed
** If you could add a line graph chart of Accuracy % By Day, you would be my hero, but don’t do if will take a long time

*Mockup*:

* None

## Acceptance Criteria
* By Day relabeled to By Transaction and grid headers updated
* New radio button / tab labeled By Day
** Columns removed as per above
** Has only one row per day
* Grids default to 10 rows
