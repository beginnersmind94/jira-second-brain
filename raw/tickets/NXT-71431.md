---
key: NXT-71431
summary: "SC - MT - District - District Boundaries History"
status: "Story Refinement/Efforting"
resolution: ""
components: ["Platform - System"]
sprints: ["Release 9.14"]
low_signal: false
---

# NXT-71431 — SC - MT - District - District Boundaries History

## Description
As a *Customer Support or Technical Support user*, I want to manage boundaries for a selected district, so that I can configure district-level access while clearly understanding whether boundaries and roles originate locally or are inherited from a parent.

----

h1. Background / Notes

We are introducing a *District Boundaries* tab on the *System > Districts* screen.

This tab allows support users to configure boundaries for a *single district (child realm)*.

Boundaries can originate from:

* *Self (district-level configuration)*
* *Parent (applied from Central Boundaries tab)*

Each boundary includes two independent inheritance controls:

# *Boundary Requirement* (Optional / Mandatory)
# *Role & Permission Requirement* (Optional / Mandatory)

Roles and permissions are treated as a *single unit* and are propagated together.

h3. Key Behavior

* *Mandatory Boundary*
** cannot be disabled at the district level
* *Optional Boundary*
** can be enabled/disabled at the district level
* *Mandatory Role & Permission*
** inherited roles are locked (cannot edit/remove)
* *Optional Role & Permission*
** roles can be modified or replaced locally

----

h1. Current Navigation

*System > Districts > District Boundaries tab*

----

h1. Technical / Implementation Notes

* Add new tab labeled *District Boundaries*
* Maintain visual consistency with *Central Boundaries* tab
* Use existing UI patterns:
** type-ahead Select Realm
** grid layout
** chip-based role selector
** confirmation dialogs
** inline validation
* Select Realm must include only realms with at least one linked parent
* Grid must include:
** Enabled
** Boundary Name
** Origin
** Boundary Requirement
** Role & Permission Requirement
** Linked Roles
* Linked Roles column should have the largest width
* Parent-inherited roles must display with suffix:
** *Role Name (Central)*
* Mandatory rows should include a visual lock indicator (e.g., 🔒 icon)
* Inherited rows should include a visual indicator (e.g., “Inherited” badge)
* Save requires validation + confirmation
* Audit log handled separately
* Boundary creation handled in *Boundary Definitions tab (separate story)*
* Parent propagation handled in Central Boundaries story

----

h1. Dependencies

* Realm relationship story
* Boundary Definitions story
* Roles & permissions system
* Central Boundaries story
* View History story

----

h1. Out of Scope

* Creating/editing/deleting boundaries
* Creating/editing roles and permissions
* Parent propagation logic
* Audit log implementation
* Boundary enforcement in modules
* Role propagation architecture details

----

h1. Functional Requirements

h2. 1. Add District Boundaries tab

Add new tab labeled *District Boundaries*

----

h2. 2. Restrict access

Accessible only to:

* Customer Support
* Technical Support

District users cannot access this tab

----

h2. 3. Select Realm field

* Type-ahead searchable dropdown
* Filters as user types
* Only shows realms with at least one linked parent

Helper text:
*“List is restricted to child realms that are linked to at least one parent or related realm.”*

----

h2. 4. Add View History button

* Visible on screen
* Behavior out of scope

----

h2. 5. Load boundaries grid

Displays:

* self-originated boundaries
* parent-inherited boundaries

----

h2. 6. Grid structure

Columns:

* Enabled
* Boundary Name
* Origin
* Boundary Requirement
* Role & Permission Requirement
* Linked Roles

----

h2. 7. Origin column

Values:

* Self
* Parent

UI Enhancement:

* Parent rows show *“Inherited” badge*

----

h2. 8. Boundary Requirement behavior

h3. Self

* Always Optional

h3. Parent

* Optional → can enable/disable
* Mandatory → cannot disable

UI:

* Mandatory rows show 🔒 icon
* Checkbox disabled for mandatory parent boundaries

----

h2. 9. Role & Permission Requirement behavior

h3. Self

* Always Optional

h3. Parent

* Optional → roles editable
* Mandatory → roles locked

UI:

* Locked roles display 🔒 icon

----

h2. 10. Enabled checkbox behavior

* Controls assignment for:
** self boundaries
** parent optional boundaries
* Disabled for parent mandatory boundaries

----

h2. 11. Role mapping behavior

* Each enabled boundary must have ≥1 role
* Roles displayed as chips
* Role includes its permissions

----

h2. 12. Inherited role display

* Parent-propagated roles must display as:
*Role Name (Central)*

----

h2. 13. Inherited role behavior

h3. Mandatory

* roles cannot be edited or removed
* selector disabled

h3. Optional

* roles can be modified or replaced

----

h2. 14. Disable role editing rules

Role selector is disabled when:

* boundary is not enabled
* role requirement is mandatory

----

h2. 15. Save Changes

* Validate before saving
* Show confirmation dialog
* Persist changes if confirmed

----

h2. 16. Validation rules

* All enabled boundaries must have ≥1 role

On failure:

* block save
* show standard error message
* highlight invalid rows

Inline message example:
*“At least one role is required for enabled boundaries.”*

----

h2. 17. Cancel / Reset

* Requires confirmation
* Reverts to last saved state

----

h2. 18. No auto-save

Changes persist only after Save + confirmation

----

h2. 19. Unsaved changes warning

* Triggered when navigating away

----

h2. 20. Bulk selection

* Select All / Deselect All available
* Mandatory boundaries unaffected

## Acceptance Criteria
* Tab is visible and correctly labeled
* Only Customer Support and Technical Support can access
* Select Realm supports type-ahead
* Select Realm only shows child realms with parent links
* Helper text is displayed
* View History button is visible
* Grid loads all relevant boundaries
* Origin column displays Self or Parent
* Parent rows display “Inherited” indicator
* Boundary Requirement behaves correctly
* Mandatory parent boundaries cannot be disabled
* Mandatory rows display lock icon
* Role & Permission Requirement behaves correctly
* Mandatory roles are locked and not editable
* Roles display as chips
* Inherited roles display with “(Central)” suffix
* Disabled boundaries disable role selection
* Enabled boundaries require at least one role
* Validation blocks save and highlights rows
* Inline validation message is shown
* Save requires confirmation
* Valid save persists data
* Cancel requires confirmation and resets changes
* Unsaved changes warning appears
* Bulk selection works without affecting mandatory boundaries
