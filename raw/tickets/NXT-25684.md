---
key: NXT-25684
summary: "INV - Orders - Production Ordering - Inventory Logic (Step 1.5)"
status: "Done Done"
resolution: "Done"
components: ["Inventory"]
sprints: ["Perseus Sprint 61(NXT Board)"]
low_signal: false
---

# NXT-25684 — INV - Orders - Production Ordering - Inventory Logic (Step 1.5)

## Description
As an Ordering Manager I want advanced Production Order logic so when I click Apply, the system figures out how much I need to order.

*Requirements*

* Load the items based on selections [https://primeroedgenext.atlassian.net/browse/NXT-23959|https://primeroedgenext.atlassian.net/browse/NXT-23959|smart-link] 
** Load all production plans for the selected date range
** load all items and usage quantities from the corresponding date range
** convert all items into quantities (from weight, etc.) - Production dependency
*** Ex: 100oz beef hamburger (Prod) > 42 beef patties (IM) > 1 Case (INV)
* Implement conversion logic on click of Apply button
** assuming no incomplete plans are found

+API Process:+

# Load Production Data
## convert all production weights, etc. into Item Management quantities (Prod team?)
## alert user to incomplete data according to Production Rules
# Reference Item Hierarchy to identify Order Item
## check item hierarchy to identify the units (Package, Case, etc.)
## check inventory contracts, to determine which Vendors to order the item from
## check Ordering Conditions, to determine which unit is orderable _for the site_
### One issue: site may be configured to order multiple hierarchical units
### Solution for now: just pick one
# Check more Inventory logic to determine Delivery Date (at this point, this should be standard inventory order logic)
## Check the corresponding Vendor's Production Lead Time value
## check the item’s (optional) Product Lead Time on the contract
# Check QOH to determine required Order Quantity
## QOH needs to be converted according to hierarchy to evaluate accurate QOH
### always show in terms of the Production Item (which is likely the lowest level item)

!image-20220124-231239.png|width=83%!

## Acceptance Criteria
* Implement the logic to identify orderable items based on production usage
