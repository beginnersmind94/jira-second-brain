---
key: NXT-9774
summary: "Data Exchange - File Import Setup: File Upload Step"
status: "Done Done"
resolution: "Done"
components: ["Platform"]
sprints: ["Perseus Sprint 34 (NXT Board)"]
low_signal: false
---

# NXT-9774 — Data Exchange - File Import Setup: File Upload Step

## Description
*User Story*

As a PrimeroEdge user, I would like to have a screen where I can quickly and easily select or drag and drop import files, so that I can more quickly upload my data.

*Mockup*

[https://www.figma.com/proto/yrkJeGfd3f9hWjqm0qvfir/Edge2.0_V1?node-id=4447%3A71492&scaling=scale-down-width|https://www.figma.com/proto/yrkJeGfd3f9hWjqm0qvfir/Edge2.0_V1?node-id=4447%3A71492&scaling=scale-down-width|smart-link] 

[https://www.figma.com/proto/yrkJeGfd3f9hWjqm0qvfir/Edge2.0_V1?node-id=107228%3A153740&scaling=scale-down-width|https://www.figma.com/proto/yrkJeGfd3f9hWjqm0qvfir/Edge2.0_V1?node-id=107228%3A153740&scaling=scale-down-width|smart-link] 

*Requirements*

* After the user advances from the Pre-Upload screen of File Import Setup, take them to the next step in the process: *File Upload*
* Here, provide users Uploading a file:
*# Drag and drop a .csv or .xlsx, or .xls (if possible) file onto the Upload Files portion of the screen
** User can ONLY one file at a time and one is file is limited
** SELECT button is enabled for user to browse the file explorer and select the file which should filter for CSV, xlsx file formats by default
* When the user has successfully added a file, replace the Upload Files section of the page with the filename and file size of the file. (file size limit 250 MB for .xls, .xlsx and .csv file)
* Upload function:
** Remove File (Trash bin icon) - Allow user to remove the file and start this step over
** When user clicks on UPLOAD:
*** Loading/Progress bar is displayed with %
*** Progress bar should disappear once the upload is complete and a DELETE icon is enabled for user to delete
*** Upload successful banner message should be displayed and disappears after 10 secs. 
*** When user clicks on DELETE
**** Display a confirmation prompt, “Are you sure you want to delete ?”
**** If user selects DELETE button, display File Upload widget
**** If user selects CANCEL, return to uploaded file widget
*** The file is validated to ensure it is the proper type, and an error is displayed if it is not. File can either be validated upon drag+drag/browse OR when selecting the Upload button--whichever is more feasible.
*** Rules for successful upload:
***# File must be .csv, .xlsx, or (if possible) .xls. 
***# File should have a reasonable size limit (architect can advise)
*** Error messages: 
**** File type must be \[support file types].
**** File size must be no larger than 250 MB.
* When user uploads the file, system should automatically detect from uploaded file and display DELIMITER And QUALIFIER
* If system is not able to detect the delimiter and qualifier, display both fields in RED and display a WARNING note
* Add a Previous button that allows the user to return to the Pre-Upload step. 
** *WARNING message that says “Going back to previous step will remove the files added for upload”*
* Only after file upload success & delimiter/qualifier are selected, Enable Next button that allows the user to proceed to the next step: *Field Mapping*

## Acceptance Criteria
* Verify that within the File Import Setup page, when the user selects the Next button on the Pre-Upload step (and conditions are right), they land on the File Upload step

* Verify that the user has 2 options for importing a file on this step:
1. Drag and drop a .csv or .xlsx, or .xls (if possible) file onto the Upload Files portion of the screen
2. Use a browser to SELECT a file from their machine - launch file explorer to open the file with file types filter

* Verify that a user can only upload a ONE file at a time

* Verify that only ONE file is allowed to upload and stage file (before uploaded to system) can be replaced

* Verify that when the user has successfully added a file, we replace the Upload Files section of the page with the filename and file size of the file.

* Verify that we once the file has been uploaded (and possibly validated at this point), we display 2 options for the user:
1. Delete File Icon - Allow user to delete the file and start this step over with File Upload drag and drop widget displayed
2. Progress bar should disappear and banner text of file upload success is displayed for 10secs

* Verify that File deletion confirmation prompt is displayed

* Verify the file is validated to ensure it is the proper type, and an error is displayed if it is not. Rules for successful upload:
1. File must be .csv, .xlsx, or (if possible) .xls. 
2. File should be less than the maximum size limit

* Verify potential error messages: 
1. File type must be [support file types].
2. File size must be no larger than [max file size limit].

* Verify that Delimiter and Qualifier is automatically detected and displayed as per the file.

* Verify when system is unable to detect Delimiter and Qualifier:
1. fields are displayed in RED & unselected
2. "Next" button disabled
3.  Warning note is displayed

* Verify each function behaves in a stepper format

* Verify that the page contains a Previous button that allows the user to return to the Pre-Upload step

*Verify that, once a valid file has been selected and uploaded, the user can continue to the Fileld Mapping step
