---
key: NXT-41106
summary: "BI Integration | Improve BI integration endpoint to reuse embedded token from cache instead of regenerating."
status: "Done Done"
resolution: "Done"
components: ["Platform - System"]
sprints: ["Perseus Sprint 98(NXT Board)"]
low_signal: false
---

# NXT-41106 — BI Integration | Improve BI integration endpoint to reuse embedded token from cache instead of regenerating.

## Description
As a developer, I would like to implement caching in BI Integration repo so that we reuse the token from the cache instead of regenerating it from Microsoft’s power bi service.

## Acceptance Criteria
When the embedded token gets stored in the cache and get reused instead of regeneration.
