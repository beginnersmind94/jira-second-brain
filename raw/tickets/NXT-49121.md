---
key: NXT-49121
summary: "eOrders Configuration - Updates"
status: "Done Done"
resolution: "Done"
components: ["Platform - System"]
sprints: ["Perseus Sprint 116 (7.5)", "Perseus Sprint 117 (7.6)"]
low_signal: false
---

# NXT-49121 — eOrders Configuration - Updates

## Description
We need to add a couple of missing fields in the eOrder configuration and make a few other small updates from user feedback

*Notes:*

* 

*Requirements:*

* These requirements apply to the menu
** Change eOrders to E-Orders
* 
* These requirements apply to System > Configuration > eOrders
** Change eOrders to E-Orders for the page name
* 
* For the Add New and Edit screens, do the following:
* When the user selects EDI 850 on the Vendor tab, it determines the fields on the Configuration tab
* On the Configuration tab, we are missing fields, so add them as follows
* Fields:
** Use Locking
*** Yes/No
*** Default is False
*** Look at the code from PrimeroEdge to see what to do with it
** Product ID Qualifier
*** Textbox 
*** Not Required
*** Look at the code from PrimeroEdge to see what to do with it
** Ship-To Department Number
*** Textbox 
*** Not Required
*** Look at the code from PrimeroEdge to see what to do with it
** Production
*** Yes/No
*** Not Required
*** Default is False
*** Look at the code from PrimeroEdge to see what to do with it
* 
* 
* When the user selects EDI (PFG) on the Vendor tab, it determines the fields on the Configuration tab
* On the Configuration tab, we are missing fields, so add them as follows
* Fields:
** Use Locking
*** Yes/No
*** Default is false
*** Look at the code from PrimeroEdge to see what to do with it
** One Order Per File
*** Yes/No
*** Not Required
*** Default is false
*** Look at the code from PrimeroEdge to see what to do with it
* 
* 
* We also need to make some code adjustments. Dev Manager will mark those here.



*Technical Details*

Backend

* Add below columns to the table (EO_EorderConfigurationDetail)
** SplitOrder
** Locking
** IsProduction
** DepartmentNumber
** ProductIdQualifier
* Modify methos, models to respect these fields (EorderConfigurationController)

FrontEnd

* As mentioned need to add additional fields on the screens and send those data to backend

## Acceptance Criteria
* Missing fields added and code modified accordingly
* Name changed in menu and title
