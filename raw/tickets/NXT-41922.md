---
key: NXT-41922
summary: "DataExchange Import – Fixed Width-Process Fixed Width files when the file is uploaded from"
status: "Done Done"
resolution: "Done"
components: ["Platform - Data Exchange"]
sprints: ["Perseus Sprint 103(NXT Board)", "Perseus Sprint 104(NXT Board)", "Perseus Sprint 105(NXT Board)", "Perseus Sprint 106(NXT Board)", "Perseus Sprint 107(NXT Board)", "Perseus Sprint 108(NXT Board)"]
low_signal: false
---

# NXT-41922 — DataExchange Import – Fixed Width-Process Fixed Width files when the file is uploaded from 

## Description
As a SchoolCafé Admin I want to import a fixed width file so that I can use my state’s existing format.

*Notes:*

* This is the backend story to use the fixed width format in an import routine
* Currently, we can only store delimited files, so we need to configure a way to use that was saved and linked to the import routine during the + New or Edit process

*Requirements*:

* Add to the import routine a way to use fixed width file formats instead of delimited files
* For now, we are starting with DC Import Minnesota fixed width, but there will be more in the future, so create in a reusable way

## Acceptance Criteria
* We are able to import fixed width formatted files
