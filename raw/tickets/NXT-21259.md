---
key: NXT-21259
summary: "Add Sites to Model Building"
status: "Story Refinement/Efforting"
resolution: ""
components: ["Face/Food Detection"]
sprints: []
low_signal: false
---

# NXT-21259 — Add Sites to Model Building

## Description
We need to update the build model code to stop blasting out the model to all registered devices and instead send to a select list of sites the user wants

*Application*: Face and Food Detection Website

*Flow:*

* No user flow for this one

 

*Background:*

* Originally, the code blasts out the model to all registered sites
* We want to be more specific about which sites we want to send a model to
* To do that, we are creating the concept of a Publisher
* This publisher will handle sending models to sites and removing models from sites

 

*Requirements:*

* Find the code that selects which sites to send a model to
* You will find it from the Create button on the Build Model page
* Update that code to look at the Model Sites table where sites are linked to a Model
* Get the devices for those sites and send the model to those sites
* Update the Start Date of the Model Sites table with the date/time stamp so that we know when it was sent to the site
* Remove the code to blast out to all registered sites
*  
* Security – already covered

 

*Mockup*:

* In Progress

## Acceptance Criteria
* Model sent to selected sites from Build Model page
* Model is not sent to any other sites other than those selected from Build Model Page
*  
* Security and roles: already covered
