---
key: NXT-16414
summary: "INV - Orders - Order Detail - Create Receipt - Save Receipt"
status: "Done Done"
resolution: "Done"
components: ["Inventory"]
sprints: ["Perseus Sprint 48(NXT Board)", "Perseus Sprint 49(NXT Board)"]
low_signal: false
---

# NXT-16414 — INV - Orders - Order Detail - Create Receipt - Save Receipt

## Description
As an operations manager I want the ability to create a receipt on a specific order so I can receive products into my inventory.

This a component of the Order Detail page, in which all details of a specific order are displayed. 
For any order that is in Open status (not Incomplete, Pending Approval, Closed), the ability to create a receipt for the order should be available. 

On the Receipt tab of the Order Detail page, there should be a ‘Create Receipt’ button, which will change the view-only state of the page into a section where items can be received by entering received quantities or checking a checkbox.

*Requirements*

* Save the Receipt when Confirm button is used
* Verify uniqueness of Invoice #
* Create batches according to item receives
* Update site inventory
* Update bin inventory
* QA will need to verify using back end

Loading header is part of [https://primeroedgenext.atlassian.net/browse/NXT-7256|https://primeroedgenext.atlassian.net/browse/NXT-7256|smart-link]

Loading items is part of [https://primeroedgenext.atlassian.net/browse/NXT-16413|https://primeroedgenext.atlassian.net/browse/NXT-16413|smart-link]

*Header*

* +Populated+:
** Site (Receipt Location)
** Vendor 
** Order #
** Order Amount
** Receipt Amount
*** (Qty * Unit Price) > rounded to 2 decimals > add all items together for Receipt Amount
** Delivery Date
* +User-Input+
** Transaction Date/Time (Received Date/Time)
*** Must be between the Order Date and the current date
**** i.e. cannot be prior to the order date, and cannot be in the future
** Order Delivered By
** Invoice #
** Invoice Amount
** Comments

*Item List*

* Grid List:
** No filters
** No columns button
** No pagination
** Item Category grouping for all items
* Receive Quantity
** Confirm checkboxes
*** to quickly confirm the auto-populated Receive Quantity
*** if Confirm checkbox is not checked, none of that item quantity will be received
** By default, Receive Quantity is auto-populated with the Due Quantity
** No restriction on quantity input
* Unit Price
** auto populated with the current contract price
** up to 4 decimal places
** editable based on permission (future, separate story)
*** for this story the price field will always be editable
* Total Price 
** (Qty * Unit Price) > rounded to 2 decimals > add all items together for Receipt Amount
* Lot Info - as a sub-grid
** Lot Number
** Date on Product (Best By, Expires, Packed)
** Date - date picker
*** no restrictions on date selection
** Receive Quantity - numeric entry field
** Add button to add another row for Lot Info
** **Note on config: currently system wide; could implement per vendor, per item, etc.
* Actions:
** Confirm Receipt
*** Prompt: If the Receipt Amount ($) does not match the Invoice Amount ($) that the user added, provide a prompt alert: “Are you sure you want to continue? The receipt amount and invoice amount do not match.”
*** Navigate back to Receipts tab once receipt is Confirmed
** Back to Order Detail/Summary (cancel receipt creation)
* Provide unsaved changes prompt if user attempts to leave page without confirming receipt, assuming some quantity entered

Mockup Notes:

* Do not need Field Search for this grid
* Do not need Columns for this grid
* Add Confirm checkbox

!Frame 1391.png|width=100%!

!Frame 1390.png|width=100%!

## Acceptance Criteria
* Receipts can be created for Orders which are in Open status
* Provide Receipt-level fields (Invoice, Total Amount, etc.)
* Provide Received Quantity fields for each item
* Provide Lot Number option for each item
* Provide warning prompt if Invoice Amount and Receipt Amount do not match
