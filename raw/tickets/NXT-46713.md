---
key: NXT-46713
summary: "DC Import - Fixed Width - New Code Mapping Fields"
status: "Done Done"
resolution: "Done"
components: ["Platform - Data Exchange"]
sprints: ["Perseus Sprint 112 (7.1)"]
low_signal: false
---

# NXT-46713 — DC Import - Fixed Width - New Code Mapping Fields

## Description
As a SchoolCafé Admin I want to be able to do code mapping on some more fields in fixed width import so that I can map my SIS values to SchoolCafé values.

*Notes:*

* We have already done this for DC Type so copy what we have there

*Requirements*:

* On DC Import fixed width imports only, do the following:
* Add a code mapping button button to the following fields:
** SNAP
** TANF
** DCMA
** DCMAR
** FDPIR
** Foster
* For each one:
** Open the code mapping drawer
*** Internal values are 0,1
*** Set title like this: *Code Mapping - DCType*
** Auto map like this:
*** Y = 1
*** N = 0
** If auto mapping will significantly add to the time to develop, then we can skip for now and come back to it later (Dev Mgr.'s call)
* 
* EligibilityType field TBD, still working with Matt on it

## Acceptance Criteria
* SNAP, TANF, DCMA, DCMAR, FDPIR, Foster have code mapping icon that opens the code mapping drawer
* Each one allows the user to manually enter code mapping values
* Each one auto maps Y to 1 and N to 0 if present in the file
*
