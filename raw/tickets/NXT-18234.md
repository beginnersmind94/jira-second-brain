---
key: NXT-18234
summary: "INV - Receipt Detail - Cleanup - Verification (UI)"
status: "Done Done"
resolution: "Done"
components: ["Inventory"]
sprints: ["Perseus Sprint 53(NXT Board)"]
low_signal: false
---

# NXT-18234 — INV - Receipt Detail - Cleanup - Verification (UI)

## Description
+Add validation messages for the below scenarios:+

# no duplicate rows are allowed with data or no data for 3 fields (Date on Product, Date Type and Lot Number).
Error Message: There are a duplicate rows with same data.
# If we have a row with all 3 fields are blank then sum of bin line quantities must have to match with item's received quantity else will display error circle icon in-front of a row whose all 3 fields are blank.
Error Message: Bin Lines quantities are not match with Item quantity.
# *“Create New Receipt”* button will be enable only if the order is in *Open* status. 

+Fix minor issues related to Receipt Detail+ (Test):

* Receipt Amount is *not* being correctly updated as values change on the page (*Done)*
** Charges and Discounts need to be reflected in the Receipt Amount value 
*** they need to be immediately reflected in the total as they are typed/entered, the same way items are
*** the check for whether or not Invoice Amount and Receipt Amount are matching must include charges and discounts
*** *Note: this is happening both on Create and Edit
** Changes in item quantity, unit price, fees and discounts, etc. are not being reflected in the Receipt Amount label
*** this defeats the purpose of the label
*** *Note: this may only be happening when editing
* Change ‘Receipt Location’ label to ‘Site’ (*Done)*
** why is there a dollar sign in front of the site?
* Charges and Discounts grid should display all charges and discounts when in edit mode (*Done*)
** current functionality limits a user to only the charge/discount they had a value for when first creating a receipt
* Charges and Discounts values should *not* included item-level fees and discounts for items that are *not* confirmed (checked)
** these calculated totals for Processing Fee, Delivery Fee, Net Off Invoice are not being correctly updated if an item is unchecked after being checked

## Acceptance Criteria
* Make UI changes to Receipt Details screen
* Verify updated points for labels, messages, etc.
