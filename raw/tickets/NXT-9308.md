---
key: NXT-9308
summary: "Data Exchange - SIS Import - Error and data integrity handling"
status: "Done Done"
resolution: "Done"
components: ["Accountability", "Platform - Data Exchange"]
sprints: ["Perseus Sprint 39 (NXT Board)", "Perseus Sprint 40(NXT Board)"]
low_signal: false
---

# NXT-9308 — Data Exchange - SIS Import - Error and data integrity handling

## Description
In this story: 

# Identify the DB/API changes that are needed to display appropriate error codes when we try to match imported patrons, like we did in Classic using FRE_PersonDetail
# Make sure error messages display in the Monitor appropriately. We can carry over Classic error messages.

Utilize this document to know what changes & messages to implement (only some of the content is relevant to Acct Mgmt, with the others being general changes that the Platform team will need to implement):

[^StudentImportMessages.xlsx]

Notes from Himaja:

_I have listed all the messages in the classic of UI/DB/Service in the attached excel file. I am not sure how we will use these messages in Perseus side, so I listed every message._

_If you need only service side messages which we logged in DE_ImportHistoryDetail table then you can filter by this table name “DE_ImportHistoryDetail” in the “Type / Table” column under Service sheet._

_In the excel file regarding the “Type / Table” column means, In service / UI we will insert the messages either in DB tables or Service System Event Log._

# _Error / Information / Confirmation Pop Up_ *_: Messages which we are shown on UI Screen_*
# _Error Log_ *_: Service System Event Log_*
# _NF_ExceptionLog / DE_JobInstanceMessage / DE_ImportHistoryDetail_ *_: Table Names_*



In this story we are going to make the changes below as well:

* Withdraw patrons for all the records in the file first.
* Changing the order of the Patrons process Withdraw/Insert/Update/Activate.

So when a file is processed, perform the withdrawals first, then insert new patron records, then update existing records, and finally activate previously-inactive patrons.

## Acceptance Criteria
This story is complete when we verify that we can identify the DB/API changes that are needed to display appropriate error codes when we try to match imported patrons, like we did in Classic using FRE_PersonDetail, and make sure error messages display in the Monitor appropriately. We can carry over Classic error messages.
