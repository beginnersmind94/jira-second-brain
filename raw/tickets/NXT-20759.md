---
key: NXT-20759
summary: "Break Up Food Training"
status: "Done Done"
resolution: "Done"
components: ["Face/Food Detection"]
sprints: ["Perseus Sprint 57(NXT Board)"]
low_signal: false
---

# NXT-20759 — Break Up Food Training

## Description
As an enhancement, we need to breakup the Food Training into 2 pages, one for New Images and one for Labeled Images and make some relevant changes to Labeled Images

*Application*: Face and Food Detection Website

*Background:*

* We accepted the functionality as-is from Amniscient in order to meet the deadline, but now need to add in at least a little bit of functionality that we originally wanted.

*Requirements*:

* Create a new link in the left menu called Trained Images and put it on the bottom
* Create a new page called Trained Images that the link goes to
* In the Food Training page, move everything from Labeled Images over to the new Trained Images page
* After doing this, there will be only New Images on Food Training page and only Labeled Images on Trained Images
*  
* For Food Training page, make the following changes
** Remove the accordion control for New Images
** If you can, don’t stretch the images, leave them their natural dimensions
** Add filters to image grid
*** filters:
*** Site
*** Date range (from and to) like you did on the Reports page
*  
* For the new Trained Images
** Page Label: Trained Images
** Remove the accordion control for New Images
** Can you change back to the grid?
*** columns:
*** Site
*** Date Trained (if you can)
*** Menu Items (like you did in Reports) (use the ones from the labeling)
** Add filters to grid
*** filters:
*** Site
*** Menu Items - like you did on the Reports page
*** Date range (from and to) - like you did on the Reports page
** ensure grid has paging, filters and export to excel
** grid default rows per page count 10 instead of 5

*Mockup*:

* None

## Acceptance Criteria
* There is a link in the left menu called Trained Images and it is on the bottom and links to that page
* In the Food Training page, everything Labeled Images is moved to the new Trained Images page
*  
* On Food Training, no accordion control for New Images
* On Food Training, has filters to image grid per requirements and they work
* On Trained Images page, has a grid with columns in requirements
* On Trained Images page, grid has filters per requirements and they work
* On Trained Images page, grid has paging, filters and export to excel
* On Trained Images page, grid default rows per page count 10 instead of 5
