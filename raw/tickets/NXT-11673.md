---
key: NXT-11673
summary: "Acct Mgmt - Settings - Add Account Management module, migrate over settings from other modules"
status: "Done Done"
resolution: "Done"
components: ["Account Management"]
sprints: ["Perseus Sprint 39 (NXT Board)", "Perseus Sprint 40(NXT Board)"]
low_signal: false
---

# NXT-11673 — Acct Mgmt - Settings - Add Account Management module, migrate over settings from other modules

## Description
The Account Management settings have already been added in the System module, but they are currently housed under the Point of Service module.

In this story, do the following:

# Add a new module to the ‘Select Module’ dropdown: *Account Management*

!image-20210304-195650.png|width=259,height=656!

2a. Move the following settings from their current locations (Point of Service and System modules) into the new Account Management module.

[^AcctMgmtSettings.xlsx]

2b. IF a setting in the list above does not exist, create it.

3. Verify that the settings still function as expected (i.e. the toggles are still toggles, the fields are still fields, the internal settings still require comments).

4. Verify the settings were _moved over_. i.e. that they can no longer be found anywhere but in the Account Management module.

We will link these settings to actual functionality in a future story.

## Acceptance Criteria
Verify that the Settings page includes an 'Account Management' module

Verify that the module includes the settings in the attached excel file, and that those settings were moved over from their previous locations

Verify that the settings can still be viewed and updated as normal
