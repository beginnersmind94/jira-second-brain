---
key: NXT-62633
summary: "GC Portal - Rule Engine on Data Mart"
status: "Release Merge"
resolution: "Done"
components: ["Global Catalog"]
sprints: ["Release 9.1"]
low_signal: true
---

# NXT-62633 — GC Portal - Rule Engine on Data Mart

## Description
_(none)_

## Acceptance Criteria
Navigation: DB Enabler

Changes Required:

A new job that runs over night and perform the following checks:

# Sort all the records based on mp_ingredientdetail_modifiedon field

# Check if Serving size is available - If Yes, update mark as review field as “Serving Size Available” (will be treated as linked ingredient or ingredient only)
## Update Rule 1 = Yes
## If serving size is unavailable - update mark as review field as “Missing Serving Size” (will be treated as Stock Items in the future), Rule 1 = Blank
# On all the items where Mark as review field  = No and uniquee field is NULL and Product Code is NOT NULL and GTIN is NOT NULL, check the following rules 
## Pick Each record and compare against the next record based on following attributes:
### Compare the 'gtin', 'ingredientname', 'amount', 'description', 'weightingrams', 'productcode'] + nutritional_columns
### Update the 1st record as Uniquee = Unique and mark the subsequent matching record as Duplicate
### Update Remarks as “Rule 2 - Match based on GTIN + Product Code + Item Name + Serving/Nutrient Data
### Capture the Key value as duplicate reference the Unique Record and all other Duplicate reference record
### Update Rule 2 = Yes

# On all the remaining records (where uniquee field is NULL and Product Code is NOT NULL and GTIN is NULL), Perform the comparison based on 'ingredientname', 'amount', 'description', 'weightingrams', 'productcode'] + nutritional_columns
## Update the 1st record as Uniquee = Unique and mark the subsequent matching record as Duplicate
## Update Remarks as “Rule 3 - Match based on Product Code + Item Name + Serving/Nutrient Data
## Capture the Key value as duplicate reference the Unique Record and all other Duplicate reference record 
## Update Rule 3 = Yes



# On all the remaining records (where uniquee field is NULL), Perform the comparison based on 'ingredientname', 'amount', 'description', 'weightingrams',  + nutritional_columns
## Update the 1st record as Uniquee = Unique and mark the subsequent matching record as Duplicate
## Update Remarks as “Rule 4 - Match based on Item Name + Serving/Nutrient Data
## Capture the Key value as duplicate reference the Unique Record and all other Duplicate reference record.
## Update Rule 4 = Yes



 5. On all the records (where uniquee field is NULL and Product Code is NULL and GTIN is NULL), Perform the comparison based on 'amount', 'description', 'weightingrams',  + nutritional_columns

        a. Update the 1st record as Uniquee = Unique and mark the subsequent matching record as “Duplicate”

        b. Update Remarks as “Rule 5 - Match based on Serving = Nutrient Data; Relevant Item”

        c. Update Rule 5 = Yes

When a record is meeting more then one rule, the remarks should be amended (Ex: Rule 1, Rule 5 etc)
