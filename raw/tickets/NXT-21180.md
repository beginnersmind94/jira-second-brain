---
key: NXT-21180
summary: "Add PUT to end point for updating previously saved custom user view."
status: "Done Done"
resolution: "Done"
components: ["Platform - Framework"]
sprints: ["Perseus Sprint 54(NXT Board)"]
low_signal: false
---

# NXT-21180 — Add PUT to end point for updating previously saved custom user view.

## Description
This would request and the url should provide the custom user view id and the request body should include the following.

# Columns: json string
# FilterCriteria: json string
# ViewName: string
# ScreenName: string
# SortBy: string (column name you would like to sot by)
# SortOrder: string (“Asc” or “Desc”).

Relative request url:

{noformat}/api/CustomView/Update/{customViewId}{noformat}

Request Type: PUT

Request body:

{noformat}{
  "filterCriteria": "string",
  "selectedColumns": "string",
  "viewName": "string",
  "dataModelName": "string",
  "sortBy": "string",
  "sortOrder": 1,
  "isDefault": true
}{noformat}

Response:

{noformat}{
  "Code": 0,
  "StatusCode": 200,
  "Description": "Success!",
  "ResponseEnvelope": null,
  "ExceptionDetails": null,
  "PayLoad": "true"
}{noformat}

## Acceptance Criteria
When I able to do the following:

# Retrieve all the custom user views for a user on a screen in a region.
