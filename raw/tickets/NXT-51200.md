---
key: NXT-51200
summary: "Imports - Item Details - Auto Field Mapping"
status: "Done Done"
resolution: "Done"
components: ["Platform - Data Exchange"]
sprints: ["Release 8.0", "Release 8.1", "Release 8.2"]
low_signal: false
---

# NXT-51200 — Imports - Item Details - Auto Field Mapping

## Description
As a Customer Support Role user, I would like to auto map the fields for Item Details imports the same we we do for API Student Imports so that I can save a lot of time and mistakes in the field mapping process

*Notes:*

* We have done auto-mapping for API Student Import
* We want to do the same thing for the File Item Details import
* -Currently, CS users have 2 different excel files they use to assemble the data - a full one and a simplified one-
* -We will use the columns from both and perform the auto mapping regardless of which is used-
* -There is a 3rd excel attachment where I did the mapping for you to follow-

*Requirements*:

* Backend:
* Add the mapping tables using the 2 sample Excel sheets for File Item Details Imports
* 
* On the System > Imports & Exports > Files tab > Add New > Item Details Imports, do the following:
* On the File Mapping tab, do the following:
* Add the auto-mapping of columns like we do for API Student Import when the user lands on the page
** Use the columns from the import file on the previous screen
* Add the Unmap and Map buttons with their related functionality the same way as in API Student Import
* Update the download file that is currently in use with the new template in all environments

## Acceptance Criteria
* When doing the import, the system auto-maps the fields for the user on page load the first time
* User can unmap all fields with a button click
* User can remap all fields with a button click
