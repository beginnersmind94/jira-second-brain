---
key: NXT-60038
summary: "Image Build: Remove hardcoded .env file and serve at runtime - Spike + Dev"
status: "Done Done"
resolution: "Done"
components: ["Platform - System"]
sprints: ["Release 8.13"]
low_signal: false
---

# NXT-60038 — Image Build: Remove hardcoded .env file and serve at runtime - Spike + Dev

## Description
# Create API {{/api/get-env}} to get APIM url dynamically during runtime
# In UIServer call {{/api/get-env}} in {{App.tsx}} during load to get APIM env and save in localstorage as {{apiInfo}}
# If APIM values exists in localstrage, use that and skip step 2
# Modify UIServer {{api-constants.ts}} to fetch APIM URL from local storage {{apiInfo}}
# Validate and remove {{process.env.APIM}} usage in all client code
# Create APIM config files in {{/client-env/<ENV>schoolcafe.json}} for all env



Part 2:

# Move {{<ENV>schoolcafe.json}} to Azure Secrets
# Fetch values from secrets
# Create a {{localschoolcafe.json}} for local env use



Part 3:

# Create single API endpoint to use as proxy for APIM from express.js
# Utilize primeroedge.js as reference for HTTP-PROXY-MIDDLEWARE
# Inject APIM_Subscription_key from express.js, remove it from Client code

## Acceptance Criteria
Remove .env and use API to serve at runtime
