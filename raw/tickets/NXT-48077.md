---
key: NXT-48077
summary: "File – Validate Data - Student Import"
status: "Done Done"
resolution: "Done"
components: ["Platform - Data Exchange"]
sprints: ["Perseus Sprint 118 (7.7)", "Release 8.0"]
low_signal: false
---

# NXT-48077 — File – Validate Data - Student Import

## Description
As a SchoolCafé admin, I would like to validate the data of a File Import so that I can see what issues I will have before implementing the import.

*Note:*

* This story is leveraging a few concepts that have already been developed:
** The File Import configuration process
** The validators that produce the exceptions grid in History log
** The notifications to user’s inbox like on the Download data feature once complete
** The download for exceptions tab in History log
** Leverage this existing code
* The use case is that the user wants to validate the data before committing to an upload and see what problems they will face
* Since it could take a long time to validate, we will instead save the exception log to a file and notify the user when it is ready for their viewing
* There is a corresponding story for API uploads

*Flow:*

* Browse to File
* Click on Validate for one of the rows that already have an import configuration completed
* User redirected to the Validate drawer
* User uploads a file with data in it and clicks Validate
* System shows message that it will be sent to their inbox and they will be notified once complete
* System performs a validation of the data as if importing, but doesn’t import
* System saves the exception analysis to the database
* If the user is still on the page, the system updates the list of previously run analysis
* The user then clicks to view the analysis button and the system loads the results in a preview grid
* User optionally downloads the results
* System also notifies the user when the file is ready by sending message to inbox and notification update
* User optionally browses to inbox and reviews the file 

*Requirements*:

* On the System > Imports & Exports > File tab > Rows, do the following:
* 
* Add a button to preview the data
** Add it to each row
** Title = Validate
* On click:
** Note: This basically is a Run Now without doing the actual import like the Download button
** Do the same validations as you do for the download button for PII and show the same error messages and confirmations
** If PII is not required, or it is and the user has it:
*** -Show the user a message:-
*** -Text: use the same text as for the Download button-
** -If confirm they want to validate, then do the following:-
*** -Close out the confirmation box-
*** -Show a message: use the same as the Download button-
*** -allow them to close it out or auto close like we normally do-
*** Open the Validate File overlay
* 
* Validate File overlay:
** Title: Validate File
** Page is in 3 sections:
*** Top left is for uploading a file and the validate button
*** Top right is for the previously validated files
*** Bottom full length is to view the validation results
** 
* Validation Section:
* Fields:
** File Upload box
*** Use the same as the Import screen
*** Do the same validations as you do for the Import screen
** Validate
*** Button
*** Disabled until they select a file
*** Put next to the File Upload
*** They cannot validate without uploading the file
*** On click:
**** Kick off the process to validate the file
**** Save the file that was uploaded for future reference
**** Do all that you do for file imports to get the data, but don’t import it 
**** Ensure you run the validations for the file that you run when you import it
**** Save the validations to the database like you do for exceptions
**** Save it in a way that can be linked to the import configuration
**** Be sure to link it to the uploaded file so that the user can see the original source data that was analyzed
**** Be sure to save the data in a way that can be shown in the dynamic grid on this page afterwards and be downloaded
**** Disable the Validate button and show the Running text and circle like we do for imports
**** Allow the user to browse away from the overlay, don’t make them wait for it to finish
**** Keep refreshing on the screen to see if the validation is done
*** Once validation is done:
**** Show it in the History section grid
**** Notify the user in a toast success message if they are still on the screen
**** Send the user a notification message via their preferences
***** The user will receive a notification animation when it is done
***** Work with System team to implement
***** User can browse to it and open it from Workspace Inbox
***** Message - Exceptions download complete. Go to the {ConfigName} import configuration and click Validate to view the file.
* 
* 
* History section:
* In this section, you will show previous validations
* Limit validations to 30 days in the past
* After 30 days, purge from the system
* All validations for this import configuration within that time frame are listed here
* If there are no validations, show a text message: No previous validations
* If there are validations, do the following:
* Show a grid:
** Data: all validations for this configuration in the last 30 days
** Default sort oldest to newest
** No paging, sorting, filters needed since will have very little data
** Columns:
** Validation Date
*** Show the date / time the validation was run
** Validated By
*** Show the name of the person that ran the validation
** Source Data
*** Show a link to the source data file that was uploaded
** Analysis
*** Each row has:
*** View
**** button
**** On click:
***** Show the validation analysis in the grid below
* 
* 
* Grid section:
* 
* If the user selects to view a validation, then show the grid
** Do it the same way you do for the dynamic Preview grid on Add New 
** Show the same grid as the Exceptions tab for Student Imports on Import Monitor
** Show the download button like you do for the API Student Import Exceptions tab and allow users to download it
** Have a clear button that will hide the preview grid if clicked

* 
* 
* Backend
* Setup a process so that after 30 days, purge old validations from the system
** Can put a trigger on the Validate button that after it is done, run a purge routine afterwards that checks for old validations and removes any source data, exception analysis and unlinks from the configuration

## Acceptance Criteria
* User can click the button to start the validate process
* User is informed of the process and can confirm
* PII view restrictions are respected for import types that are marked with it
* User is able to upload their test file
* Data is validated, but not imported
* User is notified when the file is ready to view
* User can open the file from their inbox
* Overlay shows the history of last 30 validations
* User is able to view the exception validations from previous analysis
* User can view results by clicking view
