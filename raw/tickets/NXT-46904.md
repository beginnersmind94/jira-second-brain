---
key: NXT-46904
summary: "Create a generic user across all the environments"
status: "Done Done"
resolution: "Done"
components: ["Platform - System"]
sprints: ["Perseus Sprint 113 (7.2)"]
low_signal: false
---

# NXT-46904 — Create a generic user across all the environments

## Description
Currently there is no generic user that spans across the envs and for some modules it’s causing issues as they were update the seed with createdby in lower env which  the users doesn’t exists in all other envs. This cause the data not being retrieved especially when they are doing inner joins. So they want to create a generic user that can be shared across all the envs.

Create  a post deployment script to create a record in NF_user table

username: [SchoolCafeSupport@SchoolCafe.com|mailto:SchoolCafeSupport@SchoolCafe.com]

UserId: -1

First name : SchoolCafe and Last name: Support

RegionId: -10

This needs to be run in all the environments

## Acceptance Criteria
New created user should be exists in all envs
