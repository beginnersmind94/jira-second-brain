---
key: NXT-53197
summary: "Exports - Student Balance"
status: "Done Done"
resolution: "Done"
components: ["Platform - Data Exchange"]
sprints: ["Release 8.2", "Release 8.3"]
low_signal: false
---

# NXT-53197 — Exports - Student Balance

## Description
We need to clone the student export configuration so that users can also do a separate student balance export so that users can export eligibility and balances on the same day in separate files

*Requirements*:

* For both exports, do the following:
* Clone the Student Export into a new export called Student Balances
* Ensure the database has the new Student Balance as a separate thing than Students so that if we want to add columns and things in the future, we can
* Ensure the new export shows up in the Add New Export screen and that it works properly

## Acceptance Criteria
* Users can run a student balances export as a separate export from students
* Student Balances export has all the same fields as Student export
