---
key: NXT-14856
summary: "Data Exchange - ITEMS Import"
status: "Done Done"
resolution: "Done"
components: ["Platform - Data Exchange"]
sprints: ["Perseus Sprint 45(NXT Board)", "Perseus Sprint 46(NXT Board)", "Perseus Sprint 47(NXT Board)"]
low_signal: false
---

# NXT-14856 — Data Exchange - ITEMS Import

## Description
This story is to cover the Item Import by leveraging the Import set up (File based) currently used for Student Import using files.

Here in this case, Item information is imported from external vendors using templates.

# Once Import Tab is selected, the user must be given an option of selecting the import type (Student, Items etc) and next the type of processing (API or File based). Here in this case we will select File based.
# -Since the list of fields in the template is large, grouping of fields must be done as described in the BRD.---{color:#ff5630}*This is covered in* {color}[https://primeroedgenext.atlassian.net/browse/NXT-15882|https://primeroedgenext.atlassian.net/browse/NXT-15882|smart-link]{color:#ff5630} {color}
# *Since this import is mostly by leveraging the existing platform, changes must be minimal.*
# The users who access or trigger this must be the decision makers – School District IT Director and Nutrition Director.
# Currently we are mandating customers to use the template provided by Primeroedge, the goal is to accept any type of template.
# provision must be provided so that Item import can be accessed from Item Management tab (only item import, every other import must not be enabled in this page).--{color:#ff5630}*This is covered in*{color}{color:#ff5630} {color}[https://primeroedgenext.atlassian.net/browse/NXT-15746|https://primeroedgenext.atlassian.net/browse/NXT-15746|smart-link] 
# The provision to access Item Import must be named as Import Dash board.--{color:#ff5630}*This is covered in*{color}{color:#ff5630} {color}[https://primeroedgenext.atlassian.net/browse/NXT-15746|https://primeroedgenext.atlassian.net/browse/NXT-15746|smart-link] 



[https://www.figma.com/file/8XDvpL0j1sqDskazRXlGIJ/Import-Dashboard?node-id=0%3A1|https://www.figma.com/file/8XDvpL0j1sqDskazRXlGIJ/Import-Dashboard?node-id=0%3A1|smart-link]

## Acceptance Criteria
# Verify and validate that the new changes are within the configuration module (Rename to Import Dash Board).
# Verify and validate that the user can import/download the information based on his access levels.
# Verify and Validate that all the tool tips and messages are in place.
# Verify and Validate that the Item Template is used for Mapping when Item Import and File are selected in Import Set up screen.
# Verify and Validate that grouping of source fields, mapped fields and Target fields are as per Mock up.
# The imported data must address all the required fields in the mapping spec.
# -The newly applied filters must be in place to select the type of Import “API or File Based-
# Verify and Validate if Import Dash Board button is working as expected
