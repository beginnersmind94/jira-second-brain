---
key: NXT-71249
summary: "FIN — Opening Balance Import: Data Exchange"
status: "New"
resolution: ""
components: ["Financials", "Platform - Data Exchange"]
sprints: []
low_signal: false
---

# NXT-71249 — FIN — Opening Balance Import: Data Exchange

## Description
As a district finance user, I want to import opening account balances into Financials at the start of a fiscal year or when going live on the system, so that my balance sheet and trial balance reflect accurate starting positions before any period transactions are recorded.

---

## TL;DR

Build a new **Opening Balance Import** type in Import Monitor — standard 4-tab flow. _BL owns validation and writes; DE owns UI and orchestration._

**Flow:**

* **Pre-upload:** _Fiscal Year_ dropdown + **Description** input (10–50 chars) on the left; **Download Template** link + help text on the right (matches production Import Setup layout). Eligibility query fires on FY selection; restatement gate triggers here if BL flags the FY.
* **File Upload:** `xlsx / xls / csv / txt`, **single sheet only** (reject multi-sheet at upload). **FILE DATA PREVIEW** button shows raw first rows.
* **Field Mapping:** Map 4 required targets (`GLAccount`, `SiteCode`, `Debit`, `Credit`) + 1 optional (`AccountName` — if mapped, must match the AccountName recorded for the GLAccount in CoA). Column mapping only — no validation here.
* **Review:** FY, Description, totals, effective date (from BL). User confirms → Save calls BL Commit.

**Restatement gate** fires on Pre-upload when the eligibility query flags the FY as having a **closed period** or a **prior OB import**. Gate requires the user to type the confirmation phrase `Yes, I would like to overwrite the previous import.` — Continue disabled until the typed value matches the phrase. The **Description entered on Pre-upload doubles as the restatement reason** — no separate reason field. Description + confirmation timestamp pass to BL on Commit.

**At Commit**, BL runs every business rule (per NXT-71240) in one atomic pass. If anything fails, the entire file is rejected; no ledger write. Failures surface in **Import Monitor → Exceptions tab**. If eligibility state changed between FY selection and Commit, BL blocks Commit via a modal on Review; the user re-satisfies the gate.

**Atomic.** All rows write or none. _No partial imports, ever._

**Saved mapping** auto-persists on success — auto-loads next time. Every import still goes through Commit under the same rules. **FY and Description are not persisted** — entered fresh per import.

**Roles with edit access:** Director, Central Office, SchoolCafe Admin. Customer/Technical Support always full.

---

## Requirements

### 1) Import Type

1.1 New Import Monitor Import Type **Opening Balance Import**. Help text appears in the Pre-upload right-side panel directly below the Download Template link: _"Establishes beginning balances for Assets, Liabilities, and Equity at fiscal year start or system go-live. File must have separate Debit and Credit columns."_ (per AC24)

JAS: Gap - we don't currently have help text. Resolved by RM: Help text in the Pre-upload right-side panel, matching the production Import Setup layout (per AC23 and AC24).

1.2 Supported formats: **xlsx, xls, csv, txt** (per Import Monitor patterns). - _JAS: also txt._ Resolved by RM: Added txt to supported formats.

1.3 **Roles with Edit access:** Director, Central Office, SchoolCafe Admin. Customer/Technical Support always full access (platform pattern).

_JAS: Controlled via roles and permissions. ensure there is a role with Edit access to imports/exports. Also, specify that Customer Support and Technical Support roles can do anything._ Resolved by RM: Named the specific roles with Edit access (Director, Central Office, SchoolCafe Admin). Customer/Technical Support full access noted.

1.4 **Single sheet only.** Multi-sheet files are rejected at upload. No sheet is processed. Rejection: `File contains multiple sheets. Opening Balance Import requires a single sheet. Consolidate to one sheet and re-upload.`

### 2) Prerequisites

_JAS: We don't have anything like this. it is a big deviation from out import process. Could theoretically fetch an API from Financials module with a Pass/Fail check and you can handle the logic there._ Resolved by RM: Folded into the eligibility query at FY selection (§3.3). No separate prerequisite call; no separate validation step. If the CoA lacks A/L/E accounts, the FY selection step blocks with a deep link.

### 3) Pre-upload Tab (Fiscal Year, Description, Eligibility)

_JAS: can be in the pre-upload screen after select the import type. needs to be validated by Dev Mgr. on how to do all this. Need to see this in a UX. what fields are needed? fiscal year selector, restatement textbox? there seems to be a lot of business logic on this._ Resolved by RM: FY selector + Description input live on the Pre-upload tab (left column). Eligibility query fires on FY selection. Restatement gate runs on Pre-upload when the query returns Restatement required. Right-side panel contains the Download Template link and help text per AC23/AC24.

3.1 On Pre-upload, after Import Type selection, a **Fiscal Year** dropdown lists all FYs. User picks one before uploading.

3.2 Selector does no business logic — just lists FYs.

3.3 On FY selection, DE calls an **eligibility query** to BL. _This is distinct from validation — it returns FY state at selection time, not a file processing pass._ Returns one of:

* **Eligible** — proceed.
* **Restatement required** — closed periods exist for this FY, a prior OB import exists, or both.
* **Not eligible** — prior FY has open periods, or CoA lacks A/L/E accounts.

3.4 On **Not eligible**, Next is blocked with a message and deep link (Period Management — _Financials > Configuration > Periods_ — or Chart of Accounts, depending on the cause).

3.5 On **Restatement required**, a confirmation gate appears on Pre-upload before progress. The gate:

* States which condition(s) triggered the restatement (closed periods, prior import, or both). Each trigger shows its own message:

    * Closed periods: `The selected fiscal year has one or more closed periods. To post opening balances, reopen the affected periods first.`
    * Prior OB import: `An Opening Balance Import already exists for this fiscal year. Continuing will reverse the existing entries and replace them with this import.`
    * Both triggers: both messages above are shown.
    
* When closed periods is a trigger: provides two links — `Reopen periods in Period Management` (_Financials > Configuration > Periods_) and `Post a manual adjustment instead (no period reopening required)` (_Financials > General Ledger > Add New Entry > Manual Entry_).
* For prior imports: existing OB entries will be hard-reversed and replaced on Commit (see **NXT-71240** §5).
* Requires the user to type the confirmation phrase `Yes, I would like to overwrite the previous import.` Continue is disabled until the typed value matches the phrase.
* Inline note (always shown): _"The Description you entered above will be saved as the restatement reason for audit. Edit it if needed before continuing."_ — no separate reason field.

3.6 Description and confirmation timestamp persist to the audit record and hand off to BL for ledger entry audit metadata (§10; **NXT-71240** §1.1).

3.7 All rows assigned to the selected FY (not mapped from file).

3.8 One active OB import per FY. Replacement acknowledgment goes through the restatement gate.

3.9 FY and Description selected/entered fresh per import. Not part of the persisted mapping.

3.10 At Commit, BL re-checks eligibility. If state changed since FY selection (period closed, another OB import landed), Commit is blocked. A blocking modal appears on the Review tab: `Eligibility changed since you selected the fiscal year. {Trigger}. Re-confirm the fiscal year to continue.` `{Trigger}` is one of: `A period was closed.` / `A prior Opening Balance Import was created.` / `A period was closed and a prior Opening Balance Import was created.` When the user dismisses the modal, the wizard returns to Pre-upload with the restatement gate visible. File, mapping, Description, and FY selection persist; the user must re-type the confirmation phrase to proceed.

3.11 **Description (Pre-upload).** Required text input on Pre-upload, **minimum 10 characters, maximum 50 characters**. One description for the entire import — the OB import produces a single ledger entry with many lines (per **NXT-71240** §1), and the description applies to that entry. Next disabled until Description is within the 10–50 character range.

3.12 Description is **not** a file column. If a user's file contains a column named Description, it is ignored — Description is a single wizard-entered value.

### 4) File Contract

_JAS: this would need to be on the pre upload tab after selecting the import type._ Resolved by RM: Single canonical contract — separate Debit and Credit columns required. Users conform their file to the downloadable template (§4.3). No signed-amount support and **no transformation in DE**. Files that don't conform fail per row-level validation (NXT-71240 §1).

4.1 **Single canonical format:** separate **Debit** and **Credit** columns. Each row has a positive value in exactly one of the two. Row-level rejection microcopy (BL Commit):

* Non-numeric (first 10 rows fail-fast): `Source row {RowNumber}: Debit or Credit contains non-numeric characters. Use the Download Template for the correct format.`
* Both populated: `Source row {RowNumber}: Both Debit and Credit have values. Each row must have exactly one of Debit or Credit.`
* Both blank or zero: `Source row {RowNumber}: Debit and Credit are both blank or zero. Each row must have a positive value in exactly one column.`
* Negative value: `Source row {RowNumber}: Negative value in Debit or Credit. Values must be positive.`

4.2 No user-facing amount format selector. No transformation logic in DE. Field Mapping always shows Debit and Credit as canonical target fields.

4.3 **Template available.** A **Download Template** link in the Pre-upload right-side help panel (per AC23) provides an Excel file (`.xlsx`) with the canonical headers — GLAccount, SiteCode, Debit, Credit, AccountName — and no data rows. Users conform their file to this template before upload.

### 5) Required Target Fields

_JAS: are you asking to conditionally hide/show the target fields. We don't have this for anything else. would be better to do a transformation of the data and standardize on the import contract. either combine into one or split out into 2 before proceeding with import._ Resolved by RM: Single canonical contract per §4. Conditional Amount Fields table removed. Field Mapping always shows the same set.

Required target fields on Field Mapping. Same set every import — no conditional fields. _Description is wizard-entered on Pre-upload (§3.11), not mapped from the file._

| Field | Notes |
| --- | --- |
| GLAccount | Must reference an Asset, Liability, or Equity account. Unknown account rejection: `Source row {RowNumber}: Account {GLAccount} is not in your Chart of Accounts.` Wrong type rejection: `Source row {RowNumber}: Account {GLAccount} is a Revenue or Expenditure account. Opening balances can only be set for Asset, Liability, or Equity accounts.` |
| SiteCode | Site the balance applies to. For district-wide entries, use the site code designated as the Central Office in Site Management. Unknown site rejection: `Source row {RowNumber}: Site {SiteCode} is not a recognized site.` |
| Debit | Positive value when the row is a debit; otherwise blank or zero |
| Credit | Positive value when the row is a credit; otherwise blank or zero |

Each (GLAccount, SiteCode) pair may appear only once per file. Duplicate rejection: `Source row {RowNumber}: Account {GLAccount} at site {SiteCode} appears more than once in this file. Each account may appear only once per site — consolidate to a single net balance.`

### 6) Optional Target Fields

| Field | Notes |
| --- | --- |
| AccountName | Descriptive label for the account. **If mapped, the file's AccountName must match the AccountName recorded for that GLAccount in Chart of Accounts** — BL blocks Commit on mismatch (NXT-71240 §2.5). Mismatch rejection: `Source row {RowNumber}: AccountName for account {GLAccount} in your file ("{AccountNameFile}") does not match your Chart of Accounts ("{AccountNameCoA}"). Update either the file or your Chart of Accounts before re-uploading.` The error reports both values factually; the user decides which side to update. If left unmapped, no name check runs. |

### 7) Validation

_All validation runs at BL Commit per **NXT-71240** §1–§7 (row-level rules, GL account checks, site checks, sub-account, uniqueness, file-level integrity, restatement enforcement). NXT-71240 is the single source of truth; row-level rules are not duplicated here to prevent drift. The QA-facing summary lives in this story's Acceptance Criteria (AC6–AC11, AC22, AC26)._

No data validation runs in DE. DE only collects the FY, Description, file, mapping, and restatement metadata, then hands the package to BL Commit on Save (§8, §12).

### 8) Validation Architecture

BL runs everything in one atomic Commit pass — see **NXT-71240** §0.6 + §1–§7.

8.1 No data validation in the wizard. The wizard is pure setup (Pre-upload + File Upload + Field Mapping + Review).

8.2 On Save, DE hands the mapped file, selected FY, Description, and restatement metadata (when applicable) to BL's Commit. BL runs all row-level and import-level checks in one pass.

8.3 **Atomic — all rows or none.** Any failed check rejects the entire file; no ledger write. Unbalanced file rejection: `Total debits ({TotalDebits}) do not equal total credits ({TotalCredits}). Difference: {Variance}. File rejected — debits must equal credits.` (currency values format as `$1,234.56`.)

8.4 Failures surface in **Import Monitor → Exceptions tab** per AC13. Row-level failures list every failing row with its error message. File-level failures show a single error message from the relevant AC. Commit-time eligibility changes (per AC17) surface as a blocking modal on Review, not in the Exceptions tab.

8.5 BL re-checks eligibility at Commit (§3.10). If state changed since FY selection, Commit is blocked and the user re-satisfies the gate.

### 9) File Upload, Field Mapping and Review

9.1 **File Upload tab.** A file picker. **FILE DATA PREVIEW** opens a collapsible panel with the first few parsed rows + all detected columns — raw data, no mapping. (The Download Template lives on Pre-upload's right-side panel, per §4.3 and AC23.) **Field Mapping** is column mapping UI only — no Validate button.

_JAS: the sample parsed rows we can do this on the mapping screen. usually just show first few rows. there will be no errors because we gatekeep before get to that point._ Resolved by RM: Sample rows moved to FILE DATA PREVIEW on File Upload tab per existing Import Monitor pattern. Field Mapping is column mapping only.

9.2 **Review** is the final confirm. User sees: selected FY, **Description**, totals (rows, debits, credits, parsed from file for reference), and the auto-assigned effective date (from BL).

9.3 No validation runs on Review. Save → BL Commit.

9.4 User must explicitly confirm.

_JAS (on previous §9.4 "preview must indicate direction"): Not sure about this, let's discuss your vision of what this would look like. Is this not the mapping?_ Resolved by RM: Right — Debit and Credit columns are self-evident in the file data preview. No separate "direction preview" needed.

### 10) Saved Mapping Behavior

10.1 Column mapping auto-persists on completion. Subsequent imports auto-load the previous mapping on Field Mapping. **FY and Description are entered fresh per import** — not persisted with the mapping.

10.2 Persisted mapping auto-applies, but every import still goes through Commit under the same business rules per **NXT-71240**.

_JAS: I strongly encourage adding the Validator on the import row actions. I would also add that every import must validate the file using the same rules as when creating the import._ Resolved by RM: Per-row Validate action removed — incompatible with single-Commit architecture, and no scenario produces actionable information for the user (a completed import is either posted or already returned an error report; there is no in-between state for Validate to operate on). "Every new import validates with the same rules" is honored: §10.2 enforces it.

10.3 User can modify the auto-loaded mapping on any subsequent import.

### 11) Import Results and Auditability

11.1 Each import produces a result record (status, counts, timestamps, user — Import Monitor pattern).

11.2 Import Results has two sections:

**Summary (top):**

* Status (success or failure)
* Selected Fiscal Year
* Description
* Total rows
* Total debits and credits
* Restatement indicator + confirmation timestamp (when applicable) — _restatement reason is the Description above_
* Timestamp and user

**Details (below):**

* Per-row processing details (Import Monitor pattern)

Row-level errors live in the **Exceptions tab** (per Import Monitor pattern) — not in the Results Summary.

_JAS (on previous bullet "Row-level error messages for invalid records"): should be any if doing all or nothing._ Resolved by RM: Right — atomic imports mean no row-level errors here. Failures surface in the Exceptions tab.

11.3 Import record appears in audit history showing when opening balances were set/replaced, by whom, with what description, and (for restatements) the confirmation timestamp.

### 12) Handoff to Business Logic

12.1 On Save, DE passes to BL's Commit:

* The mapped file (rows with canonical Debit/Credit columns)
* Selected fiscal year
* Entry description (10–50 chars, user-entered on Pre-upload)
* Restatement metadata when applicable (confirmation timestamp) — _Description doubles as restatement reason; not sent separately._

BL: runs all business rules in one atomic pass, assigns the effective date, creates the ledger entry and lines, persists the metadata, handles hard reversal of any previous import, makes the data available to downstream reports.

12.2 On failure, BL returns structured error details (rows + reasons). DE surfaces in Import Monitor → Exceptions tab per AC13.

## Acceptance Criteria
**AC1 — Import Type Available.** When a Finance user views Import Types in Import Monitor, Opening Balance Import appears regardless of prerequisite state.

**AC2 — Pre-upload Tab: Fiscal Year Selector + Eligibility Query.** When the user selects Opening Balance Import, the Pre-upload tab shows a Fiscal Year dropdown listing all FYs. On FY selection, DE calls BL's eligibility query and routes the user to Eligible / Not Eligible / Restatement Required behavior per §3.3–§3.5.

**AC3 — Eligibility Query — Not Eligible.** When the eligibility query returns Not Eligible, Next is blocked at FY selection. The wizard cannot progress past Pre-upload. Two triggers:

- **Chart of Accounts has no Asset, Liability, or Equity account:** the message includes a deep link to Chart of Accounts.
- **Prior FY has open periods:** the message includes a deep link to Period Management (*Financials > Configuration > Periods*).

**AC4 — Restatement Gate Triggered on Pre-upload.** When the eligibility query returns Restatement Required (closed periods in target FY, prior OB import, or both), a confirmation gate appears on Pre-upload. The user must type the phrase `Yes, I would like to overwrite the previous import.` into the confirmation field. Continue is disabled until the typed value matches the phrase.

**Trigger messages shown on the gate:**

- **Closed periods:** `The selected fiscal year has one or more closed periods. To post opening balances, reopen the affected periods first.`
- **Prior OB import:** `An Opening Balance Import already exists for this fiscal year. Continuing will reverse the existing entries and replace them with this import.`
- **Both triggers:** Both messages above are shown, and the closed-period links below also appear.

**Links shown when closed periods is a trigger:**

- `Reopen periods in Period Management` → *Financials > Configuration > Periods*
- `Post a manual adjustment instead (no period reopening required)` → *Financials > General Ledger > Add New Entry > Manual Entry*

**Inline note (always shown):** *"The Description you entered above will be saved as the restatement reason for audit. Edit it if needed before continuing."*

No separate restatement reason field is shown.

**AC5 — Required Fields Mapped.** On Field Mapping, GLAccount, SiteCode, Debit, and Credit must be mapped. Description is *not* a mapped field — it is entered on Pre-upload (AC20).

**AC6 — Decimal Format Fail-Fast.** If any of the first 10 rows has non-numeric characters in Debit or Credit, BL fails the Commit with: `Source row {RowNumber}: Debit or Credit contains non-numeric characters. Use the Download Template for the correct format.`

**AC7 — Debit/Credit Row Validity.** Each row must have exactly one of Debit or Credit positive. Failures:

- Both populated: `Source row {RowNumber}: Both Debit and Credit have values. Each row must have exactly one of Debit or Credit.`
- Both blank or zero: `Source row {RowNumber}: Debit and Credit are both blank or zero. Each row must have a positive value in exactly one column.`
- Negative value: `Source row {RowNumber}: Negative value in Debit or Credit. Values must be positive.`

**AC8 — Account Type Restriction Enforced.** Rows referencing Revenue or Expenditure GLAccounts are rejected with: `Source row {RowNumber}: Account {GLAccount} is a Revenue or Expenditure account. Opening balances can only be set for Asset, Liability, or Equity accounts.`

**AC9 — Unknown GLAccount or SiteCode Blocked.**

- Unknown GLAccount: `Source row {RowNumber}: Account {GLAccount} is not in your Chart of Accounts.`
- Unknown SiteCode: `Source row {RowNumber}: Site {SiteCode} is not a recognized site.`

**AC10 — Duplicate GLAccount + SiteCode Blocked.** Multiple rows with the same (GLAccount, SiteCode) are rejected with: `Source row {RowNumber}: Account {GLAccount} at site {SiteCode} appears more than once in this file. Each account may appear only once per site — consolidate to a single net balance.` User must consolidate to a single net balance per (account, site) before re-uploading.

**AC11 — Batch Must Balance (Strict Equality).** If total debits ≠ total credits by any amount, BL rejects the file with: `Total debits ({TotalDebits}) do not equal total credits ({TotalCredits}). Difference: {Variance}. File rejected — debits must equal credits.`Amounts formatted as `$1,234.56`. No variance permitted.

**AC12 — Atomic Import.** On any failed check or system error during Commit, the entire import rolls back; no data is written; user is notified.

**AC13 — Failure Details Appear in Exceptions Tab.** When BL Commit rejects the import (per AC12), the user navigates to **Import Monitor → Exceptions tab** to view the failure details. There is no download link.

- **Row-level failures** (per AC7, AC8, AC9, AC10, AC22): the Exceptions tab lists every failing row in the file. Each entry shows the source row number and the error message from the relevant AC. The list does not stop at the first failure.
- **File-level failures** (per AC6 non-numeric in first 10 rows, AC11 unbalanced totals, AC26 empty file): the Exceptions tab shows one error message from the relevant AC. There is no per-row list.
- **Multi-sheet rejection** (per AC19): the error appears on the File Upload tab before BL Commit runs. It does not appear in the Exceptions tab.
- **Unsupported file format** (per AC25): the error appears at the file picker before BL Commit runs. It does not appear in the Exceptions tab.
- **Commit-time eligibility change** (per AC17): the failure does not appear in the Exceptions tab. It surfaces as a blocking modal on the Review tab.

**AC14 — FILE DATA PREVIEW on File Upload.** On File Upload, FILE DATA PREVIEW opens a collapsible panel with the first few parsed rows + all detected columns — raw data, no mapping. Field Mapping shows only the column mapping UI; no Validate button.

**AC15 — Review is Final Confirm Only.** Review shows the selected FY, Description, totals (rows, debits, credits, parsed from file for reference), and the auto-assigned effective date from BL. No validation runs on Review. User must explicitly confirm; Save calls BL Commit.

**AC16 — Mapping Auto-Loads; Every Import Still Commits.** After a prior OB import, the column mapping auto-loads on Field Mapping. Every import still goes through BL Commit under the same rules. FY and Description are entered fresh per import — not persisted.

**AC17 — Commit-Time Re-Verification of FY Eligibility.** If a period closes or another OB import lands on the FY between FY selection and Commit, BL blocks Commit. A blocking modal appears on the Review tab with: `Eligibility changed since you selected the fiscal year. {Trigger}. Re-confirm the fiscal year to continue.` where `{Trigger}` is one of: `A period was closed.` / `A prior Opening Balance Import was created.` / `A period was closed and a prior Opening Balance Import was created.` When the user dismisses the modal, the wizard returns to Pre-upload with the restatement gate visible. File, mapping, Description, and FY selection persist; the user must re-type the confirmation phrase to proceed.

**AC18 — Results View Structure.** Import Results: Summary at top — status (success or failure), selected FY, Description, total rows, total debits/credits, restatement indicator + confirmation timestamp (when applicable; restatement reason is the Description), timestamp/user. Per-row processing details below, per Import Monitor pattern. Row-level errors live in the Exceptions tab — not in the Results Summary.

**AC19 — Multi-Sheet File Rejected at Upload.** Multi-sheet files are rejected at upload with: `File contains multiple sheets. Opening Balance Import requires a single sheet. Consolidate to one sheet and re-upload.` No sheet is processed.

**AC20 — Description Input on Pre-upload.** Pre-upload requires a Description input — required, 10–50 characters. Single value for the entire import (the OB import produces one ledger entry with many lines). Next disabled until Description is 10–50 chars. Description is *not* a file column; any column named Description in the uploaded file is ignored.

**AC21 — Description Doubles as Restatement Reason.** When the restatement gate fires, the Description entered on Pre-upload is saved as the restatement reason in audit metadata. No separate "Reason for restatement" field is shown. Gate displays an inline note: *"The Description you entered above will be saved as the restatement reason for audit. Edit it if needed before continuing."*

**AC22 — AccountName Match Required When Mapped.** AccountName is optional to map. If the user maps AccountName, the file's value must equal the AccountName recorded for that GLAccount in Chart of Accounts (NXT-71240 §2.5). Any mismatch rejects the row with: `Source row {RowNumber}: AccountName for account {GLAccount} in your file ("{AccountNameFile}") does not match your Chart of Accounts ("{AccountNameCoA}"). Update either the file or your Chart of Accounts before re-uploading.` The error does not assert either side is correct; the user decides whether to update the file or update CoA. If AccountName is left unmapped, no name check runs.

**AC23 — Download Template on Pre-upload.** The Pre-upload tab includes a Download Template link in the right-side help panel. Clicking it downloads an Excel file (.xlsx) named `OpeningBalanceImport_Template.xlsx` containing the canonical headers (GLAccount, SiteCode, Debit, Credit, AccountName) in row 1 and no data rows. No signed-amount transformation runs in DE; files with signed amounts in a single column fail per AC7.

**AC24 — Help Text on Pre-upload.** The Pre-upload right-side help panel shows the following text directly below the Download Template link: `Establishes beginning balances for Assets, Liabilities, and Equity at fiscal year start or system go-live. File must have separate Debit and Credit columns.`

**AC25 — Supported File Formats.** The File Upload tab accepts files with these extensions: `.xlsx`, `.xls`, `.csv`, `.txt`. Files with any other extension are rejected at the file picker with: `Unsupported file format. The Opening Balance Import accepts xlsx, xls, csv, or txt files.`

**AC26 — Empty File or Header-Only Rejected.** If the uploaded file has no data rows (an empty file or a file containing only a header row), BL fails the Commit with: `No data rows found. The file must contain at least one data row.`

## Edge / Empty / Error States

| Scenario | Behavior |
| --- | --- |
| Description blank or fewer than 10 chars on Pre-upload | Next disabled; inline validation message under the Description field |
| Description longer than 50 chars on Pre-upload | Input rejects characters beyond 50; inline counter shows current length |
| Uploaded file contains a column named "Description" | Ignored — Description is wizard-entered on Pre-upload; no warning required |
| File with extension other than xlsx, xls, csv, or txt | Rejected at file picker per AC25 |
| Prior FY has open periods | Eligibility query returns Not Eligible; Next blocked at FY selection; deep link to Period Management (*Financials > Configuration > Periods*) |
| No Asset/Liability/Equity accounts in chart of accounts | Eligibility query returns Not Eligible; Next blocked at FY selection; deep link to Chart of Accounts |
| Empty file or header-only | Commit fails per AC26 |
| Multi-sheet file uploaded | Rejected at upload per AC19 |
| Non-numeric characters in Debit or Credit (any of first 10 rows) | BL fail-fasts at Commit per AC6; entire import rejected |
| File uses signed amounts in a single column (e.g., negative = credit) | Negative values fail per AC7; entire import rejected. Error message directs user to the Download Template link on Pre-upload. |
| Both Debit and Credit populated in same row | Row rejected per AC7 (both-populated trigger) |
| Both Debit and Credit blank or zero in same row | Row rejected per AC7 (both-blank/zero trigger) |
| Negative value in Debit or Credit | Row rejected per AC7 (negative-value trigger) |
| Unknown GLAccount | Row rejected per AC9 |
| Unknown SiteCode | Row rejected per AC9 |
| Revenue or Expenditure GLAccount | Row rejected per AC8 |
| Duplicate GLAccount + SiteCode in same file | Row rejected per AC10; user must consolidate before re-uploading |
| AccountName mapped, file value differs from AccountName in CoA for the same GLAccount number | Row rejected per AC22; error reports the row, GLAccount number, file's AccountName, and CoA's AccountName for that number. User decides whether to update the file or update CoA. |
| AccountName not mapped | No name check runs; other validations apply normally |
| Selected FY has closed periods | Eligibility query returns Restatement Required; gate fires on Pre-upload (confirmation phrase required; Description above serves as the restatement reason). User must reopen affected periods through Period Management (*Financials > Configuration > Periods*) before posting. |
| Prior OB import exists for selected FY | Eligibility query returns Restatement Required; gate fires on Pre-upload. Existing entries hard-reversed and replaced on Commit (per NXT-71240 §5). |
| Period closed OR new prior import created between FY selection and Commit | Commit blocked by BL re-check per AC17; user re-satisfies the restatement gate (re-types the confirmation phrase); Description persists and remains editable. |
| File contains mix of valid and invalid rows | Commit fails per AC13; no partial import; user corrects and re-uploads |
| Total debits ≠ total credits | Commit fails per AC11 |
| System error during Commit | Entire import rolls back per AC12; no data written; user notified |

## Defaults

- **Description (Pre-upload):** Required wizard-entered field. 10–50 characters. Single value for the whole ledger entry. No default fill — user must enter.
- **AccountName:** Optional to map. If mapped, must match the AccountName recorded for the GLAccount in CoA (NXT-71240 §2.5) — mismatch blocks Commit. If unmapped, no name check runs.
- **File format:** Canonical Debit/Credit columns only. Users conform their file to the Download Template on Pre-upload (§4.3). No signed-amount transformation in DE.
- **District-wide entries:** Use the site code designated as the Central Office in Site Management. UI guidance: *"For district-wide entries, use the site code assigned to your Central Office in Site Management."*
- **Effective date:** Auto-assigned by BL (per NXT-71240) — not user-selected, not in the file.
