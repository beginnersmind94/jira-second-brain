---
key: NXT-71249
summary: "FIN — Opening Balance Import: Data Exchange"
status: "New"
resolution: ""
components: ["Financials", "Platform - Data Exchange"]
sprints: []
low_signal: false
---

# NXT-71249 — FIN — Opening Balance Import: Data Exchange

## Description
As a district finance user, I want to import opening account balances into Financials at the start of a fiscal year or when going live on the system, so that my balance sheet and trial balance reflect accurate starting positions for permanent accounts before any period transactions are recorded.

_Updated 2026-05-06._

---

## Scope

This story covers the Import Monitor pipeline for the Opening Balance Import type: import type setup, file upload, prerequisites check, fiscal year selection, column mapping, validation, preview, saved mapping, and import results. On successful confirmation, validated data is handed off to the business logic layer (companion story **NXT-71240**) for ledger entry creation.

---

## Requirements

### 1) Import Type

1.1 A new Import Monitor Import Type exists named **Opening Balance Import** with a tooltip on the Import Type selection: _"Establishes beginning balances for Assets, Liabilities, and Equity at fiscal year start or system go-live. File must have separate Debit and Credit columns."_ -

JAS: Gap - we don't currently have help text. Resolved by RM: Tooltip on Import Type selection. Trimmed to fit tooltip-sized container.

1.2 Import Type supports the following upload formats, consistent with other Import Monitor types: **xlsx, xls, csv, txt**. - _JAS: also txt._ Resolved by RM: Added txt to supported formats.

1.3 Access to the Opening Balance Import is controlled via roles and permissions. The following roles have Edit access by default: **Director**, **Central Office**, and **SchoolCafe Admin**. Customer Support and Technical Support roles have full access regardless of other role configuration, consistent with platform support patterns.

_JAS: Controlled via roles and permissions. ensure there is a role with Edit access to imports/exports. Also, specify that Customer Support and Technical Support roles can do anything._ Resolved by RM: Named the specific roles with Edit access (Director, Central Office, SchoolCafe Admin). Customer/Technical Support full access noted.

1.4 Files must contain a single sheet. Multi-sheet files are rejected at upload with a clear error directing the user to consolidate to a single sheet and re-upload. The system does not attempt to process any sheet from a multi-sheet file.

### 2) Prerequisites

_JAS: We don't have anything like this. it is a big deviation from out import process. Could theoretically fetch an API from Financials module with a Pass/Fail check and you can handle the logic there. The can be called during the validation step mentioned below._ Resolved by RM: BL exposes pass/fail API, DE consumes the result. Check runs in the validation step.

2.1 As part of the validation step (§8), DE calls a Financials BL API to check whether the prerequisite is met: at least one Asset, Liability, or Equity account exists in the chart of accounts.

2.2 If the check fails, validation fails. The user sees a blocking message linking to Chart of Accounts configuration to resolve. Progression to Review is blocked until the check passes.

2.3 Example blocking message: _"Opening Balance Import requires a chart of accounts with at least one Asset, Liability, or Equity account. Please import your chart of accounts first."_

### 3) Fiscal Year Selection

_JAS: can be in the pre-upload screen after select the import type. needs to be validated by Dev Mgr. on how to do all this. Need to see this in a UX. what fields are needed? fiscal year selector, restatement textbox? there seems to be a lot of business logic on this._ Resolved by RM: FY selector lives on the Pre-upload tab as a simple dropdown — no business logic on the selector itself. Eligibility check moves to the validation step (BL API, same pattern as §2). Restatement gate runs in validation when the API returns Restatement required.

3.1 On the Pre-upload tab, after the user selects the Opening Balance Import type, the system displays a **Fiscal Year** dropdown listing all FYs in the system. The user picks one before uploading the file.

3.2 The selector itself does no business logic — just lists FYs.

3.3 Eligibility for the selected FY is checked in the validation step (§8) via a BL API. The API returns one of:

* **Eligible** — proceed.
* **Restatement required** — closed periods exist for this FY, or a prior opening balance import exists for it (or both).

3.4 When the API returns **Restatement required**, the validation step shows a confirmation gate before the user can proceed. The gate must:

* State the conditions that triggered the restatement (closed periods, prior import, or both).
* For closed periods: tell the user they must reopen the affected periods through Period Management before the import can post; link to Period Management. Above this, surface a routing hint to the lighter-weight alternative: _"Just need to correct an account balance? Use a manual adjustment entry instead — posts in the current open period with no period reopening required."_ with a link to the manual adjustment entry flow.
* For prior imports: tell the user the existing opening balance entries will be hard-reversed and replaced (see **NXT-71240** §5).
* Require the user to type the FY identifier (e.g., `FY2025-26`) into a confirmation field. Continue stays disabled until exact match.
* Require a free-text **Reason for restatement** (min. 10 characters).

3.5 Reason and confirmation timestamp persist to the import audit record and hand off to BL for persistence on the ledger entry's audit metadata (§10; **NXT-71240** §1.1).

3.6 All rows are assigned to the selected FY. Not mapped from the file.

3.7 Each FY may have only one active opening balance import. The Restatement required gate handles replacement acknowledgment.

3.8 FY is selected fresh per import. Not part of the persisted column mapping.

3.9 At final confirm, BL is re-checked. If eligibility has changed since the gate was satisfied, confirm is blocked and the user re-satisfies the gate.

### 4) Amount Format

_JAS: this would need to be on the pre upload tab after selecting the import type._ Resolved by RM: Removed user-facing amount format selector. Single canonical contract (separate Debit/Credit columns). DE transforms signed-amount files upstream of mapping.

4.1 Files use a single canonical format with separate **Debit** and **Credit** columns. Files with a signed amount column (positive = debit, negative = credit) are supported via transformation: DE detects the shape during validation (§8) and transforms signed amounts into separate Debit/Credit values before Field Mapping.

4.2 The user is not asked to choose an amount format. The Field Mapping screen always shows Debit and Credit as the canonical target fields.

### 5) Required Target Fields

_JAS: are you asking to conditionally hide/show the target fields. We don't have this for anything else. would be better to do a transformation of the data and standardize on the import contract. either combine into one or split out into 2 before proceeding with import._ Resolved by RM: Single canonical contract per §4. Conditional Amount Fields table removed. Comments renamed to Description. Field Mapping always shows the same set.

The Field Mapping screen displays the following required target fields. The same fields appear for every Opening Balance Import — no conditional fields.

| Field | Notes |
| --- | --- |
| GLAccount | Must reference an Asset, Liability, or Equity account |
| SiteCode | Site the balance applies to. For district-wide entries, use the site code designated as the Central Office in Site Management. |
| Debit | Positive value when the row is a debit; otherwise blank or zero |
| Credit | Positive value when the row is a credit; otherwise blank or zero |
| Description | Free-text reference field, max 50 characters. Auto-filled with _"Opening Balance Import"_ if blank. |

### 6) Optional Target Fields

| Field | Notes |
| --- | --- |
| AccountName | Descriptive label for the account |

### 7) Validation Rules (row-level)

7.1 GLAccount, SiteCode, Debit, Credit, and Description must be present in the column mapping. Blank Description values are allowed at validation time and are auto-filled with _"Opening Balance Import"_ during import.

_JAS: Comment can be done during import._ Resolved by RM: Auto-fill happens at import time, not validation. Renamed Comments → Description per §5.

7.2 Debit and Credit values must be **decimal numbers**. Validation peeks at the first 10 rows during the validation step (§8); if any contain non-numeric characters in the Debit or Credit columns, the entire import is rejected with a fail-fast error indicating the user likely uploaded the wrong file.

_JAS: What happens if not in the right format? Skip the row import? must it be a decimal?_ Resolved by RM: Collapsed Scheme A/B branching now that §4 uses a single canonical contract. Spec'd decimal requirement with fail-fast first-10-rows check. "Invalid" = blocks entire import per §8.5 atomicity.

7.3 For each row, exactly one of Debit or Credit must contain a positive number. The following are invalid:

* Both Debit and Credit populated
* Both Debit and Credit blank or zero
* Negative values in either column

7.4 Zero values are not allowed and will be flagged as invalid. _JAS: what does invalid mean? don't import the row and log as import error?_ Resolved by RM: Invalid means the entire import is blocked per §8.5 atomicity. The user corrects the file and re-uploads.

7.5 GLAccount must correspond to an Asset, Liability, or Equity account in the chart of accounts. Rows referencing Revenue or Expenditure accounts are invalid and blocked with the error: _"Opening balances cannot be set for Revenue or Expenditure accounts."_

7.6 GLAccount must exist in the chart of accounts. Unknown accounts are flagged as invalid with a clear error consistent with Import Monitor patterns.

_JAS: be sure to have a mapping for this on the Target column._ Resolved by RM: Account type isn't supplied in the file — it's derived from the chart of accounts record at validation time. The terminology mapping (e.g., Fund Balance → Equity) is a great call-out, just one that would live on the chart of accounts setup flow rather than this import.

7.7 SiteCode must reference a valid site as configured in SchoolCafe. Unknown site codes are flagged as invalid. SiteCode is exposed as a mappable target field on Field Mapping (§5).

_JAS: be sure to have a mapping for this on the Target column._ Resolved by RM: SiteCode is already a mappable target field on Field Mapping per §5.

7.8 Each GLAccount + SiteCode combination must appear only once in the import file. Duplicate combinations are flagged as invalid with the error: _"Duplicate entry for account \[GLAccount\] at site \[SiteCode\]. Each account may only appear once per site."_ This is a new validator pattern — devs should treat duplicate detection as a mandatory error that blocks the entire import per §8.5 atomicity. The user must consolidate to a single net balance per account per site in their file before uploading.

_JAS: this is a new type of validator we haven't done before. Be sure to call out to the devs to not import duplicate rows and log as mandatory error._ Resolved by RM: Acknowledged as a new validator pattern. Spec'd duplicate detection as mandatory error blocking the entire import per §8.5. Auto-summing duplicates was considered but rejected — opening balance is the system's starting position, and duplicates usually indicate a problem with the export (e.g., stacked YTD + period exports) that the user should resolve before importing.

### 8) Validation Rules (import-level)

_JAS: this is a big thing actually. We don't have a place for something like this currently. You need a validator interface with the user. It will have to go on the File Upload tab after the user uploads their file. We will need a validate file button and disable the next button until it is validated. If it is successful, show user success message, if not show error message._ Resolved by RM: Added §8.1 establishing the validator gate. Existing rules renumbered. Updated 2026-05-06: Validate button moved from File Upload to Field Mapping per DE team discussion — row-level validation requires column mapping to be done first.

8.1 **Validator gate.** On the Field Mapping tab, after the user maps columns, a **Validate** button triggers a BL API call that runs all row-level (§7) and import-level (§8.2–§8.5 below) checks. The Next button to proceed to Review is disabled until validation passes. On success, the user sees a confirmation message with row count. On failure, the user sees the error reason and a download link for the error report (generated by BL) so they can correct the file and re-upload.

8.2 BL API returns pass/fail. On pass, DE displays row count confirmation. On fail, DE displays the error reason and a download link for the error report (generated by BL).

8.3 BL API blocks the import if total debits ≠ total credits. No variance is permitted — the batch must balance exactly. The blocking message is returned by BL; DE surfaces it as-is.

8.4 The user-facing acknowledgment for replacement (when a prior opening balance import already exists for the selected fiscal year) is handled by the Restatement required confirmation gate in §3.4. The reversal and re-creation of ledger entries is handled by the business logic layer — see companion story **NXT-71240** §5.

8.5 The import must be **atomic** — either all rows are imported successfully, or no rows are imported. Partial imports are not allowed. If any system-level error occurs during import processing, the entire import must roll back and the user must be notified that no data was written.

_JAS: this is critical. so you want an all or nothing import, then? then I recommend we do a validation gate-keeping after file upload and do not even let them get to file mapping screen until they have a valid file. We need to check row by row and output an error download so they can correct the file. Let's discuss. this is more than just a button with a popup._ Resolved by RM: Confirmed all-or-nothing. Validator gate (§8.1) implements the row-by-row check with downloadable error report. Updated 2026-05-06: Validator gate moved to Field Mapping (column mapping must be done before row-level validation can run).

### 9) Field Mapping and Review

9.1 The **FILE DATA PREVIEW** button on the File Upload tab opens a collapsible panel showing the first few parsed rows from the uploaded file with all detected columns. This is raw file data — no mapping applied. **Field Mapping** displays only the column mapping UI and Validate.

_JAS: the sample parsed rows we can do this on the mapping screen. usually just show first few rows. there will be no errors because we gatekeep before get to that point. the others might need to be previewed in the validation step._ Resolved by RM: Sample rows moved to FILE DATA PREVIEW on File Upload tab per existing Import Monitor pattern. Field Mapping is column mapping + Validate only.

9.2 **Review tab** is the final confirm step. After Field Mapping, the user lands on Review and sees: the selected Fiscal Year, totals (rows, total debits, total credits), and the auto-assigned effective date (provided by the business logic layer).

9.3 No validation errors appear at Review. Errors are caught and resolved at the validator gate (§8.1) on Field Mapping before the user reaches Review.

9.4 The user must explicitly confirm to perform the import.

_JAS (on previous §9.4 "preview must indicate direction"): Not sure about this, let's discuss your vision of what this would look like. Is this not the mapping?_ Resolved by RM: Right — Debit and Credit columns are self-evident in the file data preview. No separate "direction preview" needed.

### 10) Saved Mapping Behavior

10.1 The column mapping for the Opening Balance Import type is persisted automatically when the import completes. On subsequent imports, the previous column mapping auto-loads on the Field Mapping tab. FY is selected fresh per import and is not persisted with the mapping.

10.2 When using a persisted mapping, the column mapping is auto-applied — but every import must still pass through the validator gate (§8.1) using the same rules as the original import. The user uploads a new file, the validator runs all row-level (§7) and import-level (§8) checks, and Review proceeds only after validation passes.

_JAS: I strongly encourage adding the Validator on the import row actions. I would also add that every import must validate the file using the same rules as when creating the import._ Resolved by RM: Saved mapping no longer skips validation — every import passes through the validator gate (§8.1) with the same rules. Added Validate action on import row history per §10.3 below.

10.3 The Import Monitor history view exposes a **Validate** action on each prior import row, allowing the user to re-run validation against the original file without re-walking the full flow.

10.4 The user can modify the auto-loaded column mapping on any subsequent import.

### 11) Import Results and Auditability

11.1 Each import must produce an import result record consistent with Import Monitor patterns (status, counts, timestamps, user).

11.2 The Import Results view has two sections:

**Summary (top of view):**

* Success / failed status
* Selected Fiscal Year
* Total rows
* Total debits and total credits _JAS: In the summary section at top of the history?_ Resolved by RM: Yes — totals live in the Summary section at the top of the Results view.
* Restatement reason and confirmation timestamp (when applicable)

_JAS: In the summary section at top of the history?_ Resolved by RM: Yes — restatement reason and timestamp live in the Summary section at the top.

* Timestamp and user

**Details (below summary):**

* Per-row processing details consistent with Import Monitor patterns

Row-level validation errors are not shown in Import Results — those are caught at the validator gate (§8.1) before the import runs and are available via the downloadable error report at that step.

_JAS (on previous bullet "Row-level error messages for invalid records"): should be any if doing all or nothing._ Resolved by RM: Right — atomic imports mean no row-level errors appear here. Validator gate (§8.1) catches and reports those upstream via downloadable error report.

11.3 The import record must be visible in audit history so administrators can see when opening balances were set or replaced, by whom, and (for restatements) why.

### 12) Handoff to Business Logic

12.1 On successful confirmation, the data exchange layer passes the validated import data to the business logic layer (**NXT-71240**), along with the restatement audit metadata (reason text, confirmation timestamp, and trigger classification — closed periods present, prior import present, or both) when the Restatement required gate was satisfied. The business logic layer is responsible for: assigning the effective date, creating the ledger entry and lines, persisting the restatement audit metadata on the entry, handling hard reversal of any previous import, and making the data available to downstream reports.

12.2 On validation failure, BL returns structured error details (affected rows and reasons) and generates the downloadable error report. DE surfaces the download link provided by BL.

## Acceptance Criteria
**AC1 — Import Type Available.** Given an authorized Finance user opens Import Monitor, When they view Import Types, Then Opening Balance Import is available regardless of whether prerequisites are met.

**AC2 — Pre-upload Tab: Fiscal Year Selector.** Given the user selects Opening Balance Import, When the Pre-upload tab loads, Then a Fiscal Year dropdown lists all FYs in the system. The user must pick an FY before uploading the file.

**AC3 — Validator Gate.** Given the user maps columns on the Field Mapping tab, When they click Validate, Then a BL API call runs all row-level (§7) and import-level (§8) checks. The Next button to proceed to Review is disabled until validation passes. On success, the user sees a confirmation message with row count.

**AC4 — Prerequisite Check (Chart of Accounts).** Given the chart of accounts contains no Asset, Liability, or Equity accounts, When validation runs, Then validation fails with a blocking message linking to Chart of Accounts configuration. Review cannot be reached until the prerequisite is satisfied.

**AC5 — Restatement Gate Triggered.** Given the selected FY has at least one closed period OR a prior opening balance import exists for it (or both), When validation runs, Then a confirmation gate appears requiring (a) the user to type the FY identifier exactly into a confirmation field, and (b) a free-text Reason for restatement of at least 10 characters. The Continue action is disabled until both are satisfied. The gate states which condition(s) triggered the restatement and surfaces a routing hint to the manual adjustment entry flow for users who only need to correct a single account balance.

**AC6 — Restatement Audit Metadata Persisted.** Given the user satisfies the restatement gate, When the import is confirmed, Then the reason text and confirmation timestamp are persisted on the import audit record and handed off to BL for persistence on the ledger entry's audit metadata (per NXT-71240 §1.1).

**AC7 — Required Fields Mapped.** Given an upload reaches Field Mapping, When the user views the mapping UI, Then GLAccount, SiteCode, Debit, Credit, and Description must be mapped. Blank Description values are accepted at validation time and auto-filled with *"Opening Balance Import"* during import.

**AC8 — Decimal Format Fail-Fast.** Given an upload contains non-numeric characters in the Debit or Credit columns in any of the first 10 rows, When validation runs, Then validation fails fast with an error indicating the user likely uploaded the wrong file. Review is not reached.

**AC9 — Debit/Credit Row Validity.** Given a row in the uploaded file, When validation runs, Then exactly one of Debit or Credit must contain a positive number. The following are flagged invalid: both Debit and Credit populated, both blank or zero, or any negative value in either column.

**AC10 — Account Type Restriction Enforced.** Given an upload contains rows referencing Revenue or Expenditure GLAccounts, When validation runs, Then those rows are flagged invalid with the error: *"Opening balances cannot be set for Revenue or Expenditure accounts."*

**AC11 — Unknown GLAccount or SiteCode Blocked.** Given an upload contains a GLAccount that does not exist in the chart of accounts, OR a SiteCode that does not reference a valid site as configured in SchoolCafe, When validation runs, Then those rows are flagged invalid with a clear error consistent with Import Monitor patterns.

**AC12 — Duplicate GLAccount + SiteCode Blocked.** Given an upload contains more than one row for the same GLAccount + SiteCode combination, When validation runs, Then those rows are flagged invalid with the error: *"Duplicate entry for account [GLAccount] at site [SiteCode]. Each account may only appear once per site."* The user must consolidate to a single net balance per account per site before re-uploading.

**AC13 — Batch Must Balance (Strict Equality).** Given total debits ≠ total credits by any amount, When validation runs, Then BL returns a failure. The blocking message from BL is surfaced as-is by DE. No variance is permitted.

**AC14 — Atomic Import.** Given the user confirms an import and a system-level error occurs during processing, When the error is encountered, Then the entire import rolls back, no opening balance data is written, and the user is notified that the import failed.

**AC15 — Downloadable Error Report on Validation Failure.** Given validation fails with one or more row-level errors, When the user views the failure message, Then a download link for the error report (generated by BL) is available listing the affected rows and reasons so the user can correct the file and re-upload.

**AC16 — FILE DATA PREVIEW on File Upload.** Given the user uploads a file on the File Upload tab, When they click FILE DATA PREVIEW, Then a collapsible panel shows the first few parsed rows from the uploaded file with all detected columns. This is raw file data — no mapping applied. Field Mapping displays only the column mapping UI and Validate button.

**AC17 — Review is Final Confirm Only.** Given the user reaches the Review tab, When the screen loads, Then they see the selected Fiscal Year, totals (rows, total debits, total credits), and the auto-assigned effective date provided by BL. No validation errors appear at Review (caught upstream at the validator gate on Field Mapping). The user must explicitly confirm to perform the import.

**AC18 — Mapping Auto-Loads but Re-Validates.** Given a previous Opening Balance Import has been completed, When the user starts a new Opening Balance Import, Then the previous column mapping auto-loads on Field Mapping. The file must still pass through the validator gate using the same row-level (§7) and import-level (§8) rules. Fiscal Year is selected fresh per import and is not persisted with the mapping.

**AC19 — Validate Action on Import History Row.** Given a prior import exists in Import Monitor history, When the user views the history view, Then a Validate action is available on each row to re-run validation against the original file using the same rules.

**AC20 — Confirm-Time Re-Verification of FY Eligibility.** Given the user satisfied the restatement gate at validation time, and a period in the selected FY has been closed OR another opening balance import has been completed for the FY since then, When the user attempts to confirm, Then confirm is blocked with an error explaining what changed. The user must re-satisfy the restatement gate (re-type confirmation, re-enter reason).

**AC21 — Results View Structure.** Given an import completes, When the user opens import results, Then a Summary section at the top of the view shows: success/failed status, selected Fiscal Year, total rows, total debits and credits, restatement reason and confirmation timestamp (when applicable), and timestamp/user. Per-row processing details appear below the summary, consistent with Import Monitor patterns. Row-level validation errors do not appear here — those are caught at the validator gate (§8.1) before the import runs.

**AC22 — Multi-Sheet File Rejected at Upload.** Given an upload contains more than one sheet, When the file is uploaded, Then the file is rejected at upload with a clear error directing the user to consolidate to a single sheet and re-upload. The system does not attempt to process any sheet.

---

## Edge / Empty / Error States

| Scenario | Behavior |
| --- | --- |
| No Asset/Liability/Equity accounts in chart of accounts | Validation fails with blocking message and link to Chart of Accounts configuration |
| Empty file or header-only | Validation fails: *"No data rows found."* |
| Multi-sheet file uploaded | Rejected at upload with error directing user to consolidate to a single sheet |
| Non-numeric characters in Debit or Credit (any of first 10 rows) | Fail-fast: validation rejects the entire import with error indicating the user likely uploaded the wrong file |
| Both Debit and Credit populated in same row | Row invalid: *"Exactly one of Debit or Credit must have a value."* |
| Both Debit and Credit blank or zero in same row | Row invalid: *"Exactly one of Debit or Credit must have a positive value."* |
| Negative value in Debit or Credit | Row invalid: *"Debit and Credit values must be positive."* |
| Unknown GLAccount | Row invalid with clear error consistent with Import Monitor patterns |
| Unknown SiteCode | Row invalid with clear error consistent with Import Monitor patterns |
| Revenue or Expenditure GLAccount | Row invalid with explicit account type error |
| Duplicate GLAccount + SiteCode in same file | Rows flagged invalid with consolidation message; user must fix file before re-uploading |
| Selected FY has closed periods | Restatement gate triggered: typed FY confirmation and reason (min. 10 chars) required before Continue. User must reopen affected periods through Period Management before the import can post. |
| Prior opening balance import exists for selected FY | Restatement gate triggered: typed FY confirmation and reason (min. 10 chars) required before Continue. Existing entries hard-reversed and replaced on confirm (per NXT-71240 §5). |
| Period closed OR new prior import created between gate-satisfied and confirm | Confirm blocked; user must re-satisfy the restatement gate |
| File contains mix of valid and invalid rows | Validation fails with downloadable error report; no partial import allowed; user must correct the file and re-upload |
| Total debits ≠ total credits | Validation fails; blocking message from BL surfaced as-is |
| System error during import processing | Entire import rolls back; no data written; user notified of failure |

---

## Defaults

- **Description default:** Description is a required mapped field. If a row's value is blank, the system auto-fills with *"Opening Balance Import"* at import time (not validation time). Every ledger entry must carry a description.
- **AccountName:** Optional target field. Defaults to blank/null when not mapped.
- **District-wide entries:** For district-wide entries, the user should use the site code designated as the Central Office in their Site Management configuration. Import UI guidance text: *"For district-wide entries, use the site code assigned to your Central Office in Site Management."*
- **Effective date:** Auto-assigned by BL on the resulting ledger entry — not selected by the user, not in the file (per NXT-71240).
