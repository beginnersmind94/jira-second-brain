---
key: NXT-56790
summary: "PE - Pending Updates - Documents Tab (UI + API)"
status: "Release Merge"
resolution: "Done"
components: ["Global Catalog"]
sprints: ["Release 8.8"]
low_signal: true
---

# NXT-56790 — PE - Pending Updates - Documents Tab (UI + API)

## Description
_(none)_

## Acceptance Criteria
MP - GC - Pending Update:

Changes in Documents Tab:

Upon Parsing the images, the system should track individual record for updating the images.

The count of images that are not updated yet should shows under “New available Documents“ section, while application shows the local images on the left side 

Performance - on both the sections of the image, show only the first image, still populate the list of images/ names  hyperlinks, the image should be loaded only when the user click on corresponding image hyperlink or navigation control
