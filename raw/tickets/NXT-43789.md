---
key: NXT-43789
summary: "Website - Add support for Freshdesk Chat"
status: "Done Done"
resolution: "Done"
components: ["Platform - System"]
sprints: ["Perseus Sprint 106(NXT Board)", "Perseus Sprint 109(NXT Board)"]
low_signal: false
---

# NXT-43789 — Website - Add support for Freshdesk Chat

## Description
Related ticket: [https://primeroedge.freshdesk.com/a/tickets/210558|https://primeroedge.freshdesk.com/a/tickets/210558] 

# Let’s add code to display the chat icon for Freshdesk. 
## The icon can continue being displayed in the bottom-left of every screen once the user has logged in and if their district has the (internal) setting enabled. If we need to combine this feature with the existing Help button, that is fine also.
## Classic dev team should be able to provide the line of code to enable Freshchat. We should display the icon in our primary color if possible.
# Add the internal system setting: “Enable Chat Button”. This can be disabled by default as we will likely make it a paid feature in the future. When enabled, display the chat icon above, otherwise hide it. 
## We will also tie this to user role in the future so please keep that in mind (maybe only managers or admins can chat in a subsequent release, for example).

More details will be added by Sprint 106.



Summary after Nov. 16th call with Freshdesk and discussion with Ravi, Hari, and Narni:

Two options:

# (Preferred) Hide 'Help' button and call it when the user clicks the (?) button on the top-right of the screen (next to User Profile). In place of that previous Help button, display the new Chat button. This can be shown/hidden via setting. So there would only be 1 button visible on the bottom-left (unless disabled by the district), with 1 button hidden and called separately. --*OR--*
# Display the chat button on top of the existing 'Help' button, but reduce the size of the buttons such that they do not hide the nav icons for most screen resolutions (tablet and larger)

## Acceptance Criteria
Verify that an internal setting to enable Freshdesk chat has been added.

Verify that setting is disabled by default.

Verify new Freshdesk chat icon displays and functions in the same way it does in Classic, when the setting is enabled for a district (any user can chat for now).
