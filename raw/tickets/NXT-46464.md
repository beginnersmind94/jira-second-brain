---
key: NXT-46464
summary: "Data - PowerQuery - Add New"
status: "Story Refinement/Efforting"
resolution: ""
components: ["Platform - Data Exchange"]
sprints: []
low_signal: false
---

# NXT-46464 — Data - PowerQuery - Add New

## Description
As a SchoolCafé user, I want to be able to run PowerSchool PowerQueries so that I can retrieve specific data that I need to see.

*Notes:*

* This story can only be done after the POC to investigate how to connect to PowerSchool API and run a PowerQuery
* Here we are implementing a frontend for running PowerQuery



*Requirements*:

* On the System > Imports & Exports > Data tab, do the following:
** If user clicks PowerQuery in the + New button, open the Add New PowerQuery drawer



*Add New PowerQuery drawer*

* If the drawer does not already exist, create it
** Title: Add New PowerQuery
** Width: full page because of the grid
** Use sliding motion to open
** Make UI consistent with the Add New SIS Source Data drawer
* 
* Fields:
** PowerQuery Name
*** single line textbox
*** Use same requirements as Name field on Review tab for + New Imports
*** Make same as on the Add New SIS Source Data drawer
** Description
*** Multi-line textbox
*** Optional
*** Make same as on the Add New SIS Source Data drawer
** {color:#ff5630}URL - Do like SIS Configuration (see screenshot){color}
** {color:#ff5630}Key - Do like SIS Configuration (see screenshot){color}
** {color:#ff5630}Secret - Do like SIS Configuration (see screenshot){color}
** {color:#ff5630}Test Connection and info message - Do like SIS Configuration (see screenshot){color}
** 
!image-20240529-193438.png|width=460,height=361,alt="image-20240529-193438.png"!
** -Source Data-
*** -Make read only-
*** -Only value: PowerQuery and it is selected by default-
*** -Go ahead and show the field to keep UI consistent with the other screen-
** {color:#ff5630}Arguments{color}
*** {color:#ff5630}Inline editing grid{color}
*** {color:#ff5630}Has ability to add, edit, delete{color}
*** {color:#ff5630}No paging, filtering, sorting is needed but ok{color}
*** {color:#ff5630}Columns:{color}
**** {color:#ff5630}Field Name{color}
***** {color:#ff5630}Textbox{color}
***** {color:#ff5630}Required{color}
***** {color:#ff5630}Max Length: 150???{color}
**** {color:#ff5630}Value{color}
***** {color:#ff5630}Textbox{color}
***** {color:#ff5630}Required{color}
***** {color:#ff5630}Max Length: 150???{color}
** Source Data Has Personally Identifiable Information (PII)
*** Checkbox
*** User can check  / uncheck as needed
*** Default value is unchecked
** -Examples-
*** -Single select drop down-
**** -Can be a single select list or an html list-
*** -Put next to Query-
**** -User clicks on one and it loads the sample query text into the Query textbox-
**** -This helps them so that they can prove it works and they won’t have to start from scratch-
** -Query-
*** -Multi-line textbox-
*** -Make it large, high and wide-
*** -Default the text with a simple query that the user can execute successfully-
** Preview
*** Button
*** Do just like the Preview button in the API Add New
*** On click:
**** Execute the API call to the PowerSchool API using the info provided the same way you do for the existing previews
**** Show the spinning wheel while executing the query
**** Show the result(s) in the grid below like already do for other stuff
**** If get errors, show the message to the user so that they can try to correct it
*** Ensure IsPII if Source Data has IsPII setting in DB
**** User cannot preview data if data IsPII and user is not (but they can still create the config)
**** If they try to preview and can’t, show them the IsPII violation message like we do for API imports
** Save
*** On click:
**** Validate fields
**** Save to DB
**** Show green success message
**** Refresh grid in Data tab to show now row
* 
* When saving, save to the history audit log as well so that it will show up in the View History drawer
*  
* Security
** Need Add rights to Imports & Exports page
* 
* Backend work
** Create a table to store a list of sample PowerQuery texts that can be used on the front end so the user doesn’t have to start from scratch
** Create the API connection configuration stuff necessary to query the PowerSchool APIs
** Create the tables and APIs necessary to save, edit and delete PowerQueries per district

## Acceptance Criteria
* User is able to select a data source
* User is able to select a PowerQuery
* User can provide arguments
* Users can execute a PowerQuery and see the results in the preview
* IsPII checkbox is respected for previewing data
* User must have Add rights to parent page
